# 邀请合伙人无限层返佣系统

# 业务定义

## 1\.1 核心业务模型

**模型定义：无限级差佣金制**

- 平台为每个一级合伙人设定一个返佣比例上限（运营可在权限后台配置，当前原型演示为 **80%**），该比例即为此合伙人整条业务线的「总佣金包」上限参考。

- 合伙人可以无限层级地发展下级合伙人，每一级合伙人的返佣比例由其直属上级设定，且必须**严格低于**上级自身的比例（形成正级差）。

- 当终端用户（直客或合伙人自营）产生交易手续费时，系统沿邀请链路向上逐级计算级差佣金，直到一级合伙人为止。

**角色定义**

|角色|账号类型|交易时身份|是否参与返佣分配|
|---|---|---|---|
|一级合伙人|Agent|渠道/交易者|是，享受自返佣，并从下级合伙人的级差中获利|
|N级合伙人 \(N≥2\)|Agent|渠道/交易者|是，享受自返佣，并从直属下级合伙人的级差中获利|
|直客|Client|纯交易者|是，为上级合伙人贡献级差的「燃料」，自己不拿返佣|

**核心原则：合伙人自营也参与返佣**

- 合伙人用自己的合伙人账号交易，自己拿走自己返佣比例对应的佣金（自返佣）。

- 同时，他的所有上级依然按级差规则分钱。

## 1\.2 返佣计算规则

### 规则一：自返佣

- 触发条件：代理使用自己的代理账号进行交易并产生手续费。

- 计算：`该代理的返佣比例 × 该笔手续费`

- 拿到钱的人：该代理本人。

### 规则二：直客级差

- 触发条件：直客进行交易并产生手续费。

- 计算：

    - 直客的直属上级代理：拿走自己**全额**返佣比例对应的佣金。

    - 该上级的每一位上级：拿走 `(自己的返佣比例 - 直属下级代理的返佣比例)` 的级差部分。

- 如果上级与直属下级的比例相同，则该上级在这笔直客交易上级差为 0。

### 规则三：代理自营级差

- 触发条件：某代理用自己代理号交易产生手续费。

- 该代理自己先拿走自返佣（规则一）。

- 他的所有上级，按规则二的方式，从他身上计算级差，即 `(上级比例 - 该代理比例) × 该笔手续费`。

### **公式总结：**

- 对任意一笔交易：

    1. 确定交易发起人 `X`（可能是代理或直客）。

    2. 如果 `X` 是代理，`X` 获得 `比例_X × 手续费`。

    3. 沿着 `X` 的上级链路向上遍历，当前上级 `P`：

        - 如果 `P` 是 `X` 的直属上级，`P` 获得 `(比例_P - 比例_X) × 手续费`

        - 否则，`P` 获得 `(比例_P - 比例_直属下级) × 手续费`

    4. 直到一级代理停止。

## 1\.3 返佣结算流程

**整体流程：日结制**

1. **交易数据采集**：系统实时记录每一笔交易的手续费。

2. **每日结算触发**：每日 00:00 \(UTC\+8\)，系统对前一日 00:00:00 至 23:59:59 的所有交易进行结算。

3. **数据同步**：生成用于审查的四个 CSV 文件，进入风控审批队列。

4. **人工审查**：风控或运营人员登录后台「佣金对账与发放」页面，进行四步审计：

    - **总量对账**：用 Platform\_Summary 核对平台总手续费与总返佣预算。

    - **聚合校验**：用 Rebate\_Split\_Detail 的流水表通过 Excel 枢纽分析，反向汇总每个代理应得总金额。

    - **差异比对**：将汇总结果与 System\_Proposed\_Payout 做 VLOOKUP 比对，排查是否存在非零差异。

    - **穿透抽样**：随机抽检一个代理，验证其分账流水是否与 Affiliate\_Map 中的比例关系吻合。

5. **审批执行**：

    - **通过**：风控点击「审核通过并放款」，系统将最终实发金额执行发放。

    - **拒绝**：风控填写拒绝原因，该批次标记为「已拒绝」，打回重算。

6. **数据归档**：所有 CSV 文件和审批记录存档备查。

## 1\.4 返佣比例调整、关系迁移与异常配置保护

### 比例倒挂（级差异常）

- **比例调整触发检查**：当任何代理的返佣比例被调低时（平台调一级、上级调下级、或在返佣树内批量修改），系统必须实时检查该代理名下**所有直属下级**的返佣比例。

- **异常判定标准**：若存在 `下级返佣比例 ≥ 上级返佣比例`（无正级差或倒挂），则判定为**配置异常**。

- **异常影响范围**：以发生倒挂的**该返佣分支**为起点（该上下级节点及其下级），从异常当下起暂停该分支的返佣结算。**并非**整棵一级合伙人树下所有分支停结；同一一级下其他正常分支继续结算。

- **异常状态标记**：该返佣分支上的合伙人标记为 `待修正返佣后计算`（一级根节点若仅部分分支异常，可显示 `部分分支异常`）。异常期间该分支交易额计入「异常暂停交易额」；「异常返佣线」为倒挂的上下级地址对数量。

### 修正与恢复

- 运营需进入「合伙人管理」或「返佣关系树」，找到比例不合规的下级，**手动将其比例调低至合规**（下级比例严格低于上级）。

- 一旦该合伙人名下所有直属下级比例均 **&lt;** 自身比例，系统自动解除异常标记，**立即恢复**结算资格。

- 恢复后，下一个结算周期（每日 00:00）将对**该链路从异常开始到恢复正常期间所有未结算的交易**，用**修正后的新比例**重新计算佣金，并合并到当日或后续批次中发放（用户端与后台结算流水可显示为「补结算」；后台「修正返佣补发」Tab 记录原应结日与补发金额）。

### 运营权限上限与审批

- 每位市场运营在权限配置中设有 **一级合伙人返佣比例上限（%）**（原型演示为 **80%**），页面顶栏展示「权限上限: XX%」。

- **≤ 运营上限** 的比例调整：在返佣树内提交后**立即生效**（无需审批）。

- **&gt; 运营上限** 的一级绑定或比例调整：须提交审批，流程为 **市场运营提交 → 风控审核 → 老板审批**（风控通过后同步 Lark，老板可在 Lark 或后台通过）。

- **同一批提交中若混合「权限内」与「超上限」修改**：仅超上限项进入审批；权限内修改**自动放弃**（输入框回滚），须先处理超上限申请。

### 返佣关系迁移

- 运营可将某用户（普通用户或代理用户）及其伞下关系整体迁移到新的上级合伙人之下，并重新设定迁移主体的返佣比例。

- 迁移后须满足：迁移比例 ≤ 目标上级比例、≤ 运营上限、且迁移后整伞内任意父子节点仍保持严格级差。

- 若迁移主体为代理用户，其原有下级树中若存在**深层倒挂**（与本次迁移比例无关的历史问题），须在预览树中先修正再允许提交。

- 迁移申请提交后进入 **风控审核**（无需老板审批）；通过后执行关系变更。

## 1\.5 其他补充

### **活跃用户 / 活跃代理定义**

- 定义为「所选时间周期内，交易额 &gt; 0 的用户或代理」。

- 此指标为**查询时实时计算**，不需要数据落库，不需要维护每日活跃状态表。

- 举例：选择统计周期为「1W」，则统计近 7 天内有过至少一笔交易（交易额 &gt; 0）的用户/代理数量。

### **净收入（后台指标）**

- 对任意查询根节点（某一级或下级合伙人）：`净收入 = 该伞下净手续费 − 由该伞下交易触发的全部返佣支出`。

- 「全部返佣」包含分给伞内所有合伙人，以及分给伞外上级合伙人的级差（因这些级差均由伞下交易手续费产生）。

- 一级合伙人视角即整条业务线平台留存；下级合伙人视角为「以该合伙人为根的子树」对平台整体的净贡献。

---

# 后台管理：合伙人管理及结算对账

本后台面向两类内部人员：

- **运营/BD 同学**：负责配置和管理一级合伙人、调整返佣树比例、发起关系迁移（仅可见自己负责的数据）。

- **超级管理员 / 风控**：负责维护后台人员与页面权限（含一级合伙人返佣比例上限，见《[后台权限与角色管理](后台权限与角色管理.md)》），执行合伙人相关审批，以及每日佣金对账与发放。

## 侧栏结构

后台采用 **ForX Admin 统一侧栏**（见《[后台权限与角色管理](后台权限与角色管理.md)》§3）。

| 侧栏入口 | 页面 | 对应本文章节 |
|---|---|---|
| 合伙人管理 | 一级合伙人列表、任意层级查询、详情、返佣树 | §2.1、§2.2 |
| 迁移返佣关系 | 整伞关系迁移与预览、倒挂修正 | §2.3 |
| 合伙人审核 | 超上限绑定、超上限比例调整、关系迁移审批 | §2.4 |
| 佣金对账与发放 | 每日佣金审计、实发/补发调整与放款 | §2.6 |
| 操作记录 | 合伙人相关审批操作审计日志 | §2.5 |

**产品原型**：[代理中心后台](https://dashuaibizzy.github.io/prototype/perp_dex/admin/代理中心后台.html)

**权限**（与权限后台联动，原型全量展示）：

| Tab | 读 | 写 |
|---|---|---|
| 一级合伙人管理 | 查看合伙人与团队数据（运营仅看自己配置的） | 绑定一级合伙人；在返佣树调整比例；发起迁移返佣关系 |
| 合伙人审核 | 查看审批单 | 风控/老板在对应角色下审批（运营提交） |
| 佣金对账与发放 | 查看对账数据 | 执行对账、调减实发/补发、放款或拒绝 |

运营账号及返佣配置上限已合并至「权限与用户」模块。详见《[后台权限与角色管理](后台权限与角色管理.md)》§4.1、§7。

## 通用交互约定

### 统计周期

列表页与详情页均提供统计周期切换：**1D / 1W / 1M / 3M / ALL**（默认 ALL）。切换后，团队总交易额、手续费、返佣、净收入等周期敏感指标同步刷新。列表与详情打开时默认继承列表当前周期。

### 分页

合伙人列表、详情子表、返佣树一级支路、迁移预览树、结算批次与明细、操作记录、审批列表等长列表统一 **每页 10 条**，提供页码切换（含省略号大页数样式）。

### 地址与 UID 复制

钱包地址、UID 在列表、详情、树节点、结算、审批详情等处以缩写展示，并提供一键复制完整值，复制成功有短暂提示。

### 页面路由（Hash）

支持浏览器 Hash 直达：`#agent`（合伙人列表）、`#partner-detail`（合伙人详情）、`#rebate-tree`（返佣关系树）、`#migrate`（迁移返佣关系）、`#approval`（合伙人审核）、`#settlement`（佣金对账）、`#logs`（操作记录）。侧栏点击与页内跳转同步更新 Hash；进入返佣树时保留 `#rebate-tree`。

---

## 2\.1 合伙人管理页

**访问路径**：侧边栏 → 「合伙人管理」

**页面作用**：查询一级合伙人及向下伞数据；进入详情查看团队构成；进入返佣树修改比例；新增一级合伙人。

**页头提示**：展示当前登录运营的 **权限上限（%）**。

### 2\.1\.1 查询与筛选

| 筛选项 | 说明 |
|---|---|
| 钱包 / UID / 备注 | 支持任意层级合伙人模糊查询（命中后展示对应记录） |
| 结算状态 | 全部 / 正常 / 异常（含分支异常、待修正返佣后计算等非正常状态） |
| 统计周期 | 1D / 1W / 1M / 3M / ALL，影响列表金额列 |

**口径说明（列表区脚注）**：

- 团队指标均为该用户**向下整伞**汇总。

- 净收入 = 伞下净手续费 − 由伞下交易触发的**全部返佣**（含向上级级差）。

- 异常仅暂停**相关返佣分支**，非整棵一级树。

- 系统每 **5 分钟**校验级差关系。

### 2\.1\.2 查询列表

默认展示一级合伙人；搜索可定位到任意层级合伙人记录。

| 列名 | 说明 |
|---|---|
| 合伙人 | 钱包、UID（可复制）；备注名；点击进入详情 |
| 层级 | 平台层级 L1–L5；展示**直属**下级合伙人数 |
| 返佣比例 | 当前配置比例 |
| 结算状态 | `正常结算` / `部分分支异常` / `待修正返佣后计算` |
| 异常暂停交易额 | 仅该用户关联异常分支的暂停交易额；无则 `--` |
| 异常线 | 倒挂上下级地址对条数 |
| 团队总交易额 | 当前统计周期内向下伞总成交额；**可排序** |
| 手续费 | 当前周期伞下手续费；**可排序** |
| 返佣 | 当前周期伞下返佣；**可排序** |
| 净收入 | 当前周期净收入；**可排序**；数据不可用时可 `--` |
| 团队净入金 | 向下伞净入金 |
| 用户规模 | 活跃人数 / 总人数（向下伞） |
| 配置运营 | 一级合伙人显示负责运营邮箱；非一级显示 `—` |
| 操作 | 【详情】【返佣树】 |

**排序**：点击「团队总交易额 / 手续费 / 返佣 / 净收入」列头切换升序/降序（默认降序）。

**操作**：【+ 新增一级合伙人】打开绑定弹窗。

### 2\.1\.3 新增一级合伙人（弹窗）

| 字段 | 规则 |
|---|---|
| 钱包 / UID | 必填；纯数字输入视为 UID |
| 返佣比例 (%) | 必填 |
| 申请备注 | **必填**，说明绑定原因、渠道背景 |
| 图片附件 | 选填，最多 **4 张**图片（渠道协议、身份证明等） |

**提交逻辑**：

- 返佣比例 **≤ 运营配置上限**：直接绑定生效（生产环境落库；原型演示为成功提示）。

- 返佣比例 **&gt; 运营配置上限**：生成审批单「**一级合伙人绑定（超上限）**」，进入合伙人审核（风控 + 老板）；附件与备注写入审批详情。

### 2\.1\.4 合伙人详情

对齐 Web「合伙人中心」数据总览布局。

**顶部操作**：

- 【← 返回】回到列表。

- 【查看异常返佣线】：当该一级伞下存在异常记录时显示；弹窗列出异常地址对，可定位到返佣树。

- 【返佣树 / 修改比例】：进入返佣关系树（以该合伙人为焦点）。

**异常横幅**（分支级）：

- 标题：结算异常（返佣分支级暂停）。

- 展示异常暂停交易额、异常线条数。

- 说明：仅相关分支暂停结算，其他分支正常结算。

**统计周期**：与列表相同（1D / 1W / 1M / 3M / ALL）。

**指标卡片**：

| 指标 | 说明 |
|---|---|
| 团队总交易额 | 向下伞成交额（当前周期） |
| 手续费 | 向下伞手续费（当前周期） |
| 总返佣收入（本期预估） | 拆分：自返佣 / 直客 / 级差 |
| 净收入 | 伞下净手续费 − 伞下触发的全部返佣 |
| 团队净入金 | 向下伞净入金 |
| 活跃/总人数（向下伞） | 活跃 = 周期内交易额 &gt; 0 |

**Tab：直属下级合伙人明细**

| 列名 | 说明 |
|---|---|
| 加入时间 | 绑定时间 |
| 下级合伙人 | 钱包、备注 |
| 设置比例 | 异常时红色高亮 |
| 级差 | 上级比例 − 下级比例；倒挂为红色负值 |
| 贡献级差收入 | 异常时 `-- 暂停结算` |
| 总交易额 | 该下级向下伞成交额 |
| 净入金 | 该下级向下伞净入金 |
| 用户规模 | 总人数 / 活跃 |
| 结算状态 | 同列表状态枚举 |

**Tab：自邀直客明细**

| 列名 | 说明 |
|---|---|
| 注册时间 | |
| 直客钱包/UID | |
| 累计交易额 | |
| 累计手续费 | |
| 贡献返佣 | |
| 状态 | 如「交易中」 |

表格上方支持「搜索地址或备注」过滤两个 Tab 内容。各 Tab 与佣金结算记录均分页（10 条/页）。

**佣金结算记录**

| 列名 | 说明 |
|---|---|
| 结算日期 | |
| 有效交易额 | |
| 应结佣金 | |
| 原始佣金 | 异常时为「待修正返佣后计算」 |
| 状态 | `已发放` / `补结算` / `待修正返佣后计算` 等 |
| 备注 | 如比例倒挂、异常分支 |

---

## 2\.2 返佣关系树

**入口**：合伙人列表【返佣树】、详情【返佣树 / 修改比例】、异常记录定位。

**核心原则**：无论从哪一层合伙人进入，树始终**锚定在该用户所属的一级合伙人根节点（L1 伞）**上展示完整业务线；进入深层的合伙人时，自动**定位并高亮**该节点，而非只展示以该节点为根的子树片段。

### 2\.2\.1 页面结构

- **标题**：展示焦点合伙人备注 + 所属 L1 备注 +「返佣树」。

- **异常返佣线区块**（若存在）：表格列出倒挂的上下级地址对、暂停交易额；【修正返佣】可定位到树中下级节点。

- **待提交修改条**：暂存未提交的比例变更，标注「权限内 · 立即生效」或「超权限 · 提交审批」。

- **操作说明**：上级链固定展示；下级默认只展示直属合伙人，点 **+** 逐层展开；多条修改后统一【提交修改】。

- **定位搜索**：在当前 L1 伞内按钱包 / UID 搜索；自动展开路径、翻页到正确的一级直属支路页、高亮节点并滚动到视区。**不会**因定位而折叠整树为单一路径。

### 2\.2\.2 树形展示规则

**一级入口节点**：L1 根固定在顶部；当焦点为 L1 时标记「当前」。

**上级链**：自焦点向上追溯至 L1，完整展示（无旁支）；链上节点均可编辑比例。

**向下展开**：

- **一级直属支路分页**：L1 的**直接下级**合伙人按支路分页展示（10 条/页），每支路可 **+ / −** 展开更深层级。

- 每个节点展示：层级、钱包、UID、备注、可编辑的返佣比例输入框。

**级差规则**：下级返佣比例必须**严格低于**直属上级。在输入框暂存变更时，若对含下级的节点**下调**比例，弹出提示列出直属下级当前比例，提醒确认级差影响。

### 2\.2\.3 比例修改与提交

运营在树上修改一个或多个节点比例后，变更进入「待提交修改」列表：

| 变更类型 | 新比例 vs 运营上限 | 提交后行为 |
|---|---|---|
| 权限内 | ≤ 运营上限 | 确认后立即生效 |
| 超权限 | &gt; 运营上限 | 每项生成一条「返佣比例调整（超出上限）」审批单 |

**确认提交弹窗**（点击【提交修改】后）：

| 元素 | 规则 |
|---|---|
| 变更清单 | 每条展示钱包、原比例 → 新比例、生效方式标签 |
| 混合批提示 | 若同时存在权限内与超权限项：提示仅超权限项进审批，权限内项将**自动放弃**并回滚输入框 |
| 修改原因备注 | **必填** |
| 图片附件 | 最多 **4 张**图片（PNG 等）；超上限审批时随审批单保存，供风控/老板查看 |

**确认逻辑**：

- 全部为权限内：确认后批量生效；若含下调且该节点有下级，须二次确认级差风险。

- 全部为超权限：仅提交审批，不立即改比例。

- 混合：用户确认后，超权限项提交审批；权限内项放弃并回滚。

- 超权限审批通过后，由后台执行比例更新（与一级绑定超上限审批同理）。

---

## 2\.3 迁移返佣关系

**访问路径**：侧栏 → 「迁移返佣关系」

**页面作用**：将普通用户或代理用户（及其伞下关系）整体迁移到新的上级合伙人之下，并设定迁移后的返佣比例；提交前须在预览中完成倒挂校验与修正。

### 2\.3\.1 录入区

| 字段 | 说明 |
|---|---|
| 待迁移用户 | 钱包或 UID；系统自动识别主体类型 |
| 迁移到上级合伙人 | 目标上级钱包或 UID |
| 设置返佣比例 (%) | 迁移主体在新上级下的比例；UI 范围 1–80（与运营上限一致） |

输入后实时进入**预览区**（主体未识别时预览为空）。

### 2\.3\.2 主体类型与迁移范围

| 类型 | 识别结果 | 迁移范围 |
|---|---|---|
| 普通用户 | 非代理账号 | 仅该用户本人；预览展示其直客列表（分页） |
| 代理用户 | 代理账号 | **整伞迁移**：该代理及其全部下级合伙人、伞下全部直客一并挂到新上级 |

预览卡片展示：类型标签、钱包、UID、系统层级 Lx、链路规模、直客数量等。

### 2\.3\.3 预览：代理用户链路树

- 以**迁移主体为根**展示代理链路树（相对标签：迁移主体 / 直接下级 / 二级下级…），并标注**系统 Lx**。

- 根节点比例 = 录入的「设置返佣比例」；根不可在树内改比例（改录入框）。

- 非根节点可在树内修改比例（用于修正倒挂或调整下级级差）。

- 迁移主体的**直接下级**支路分页（10 条/页）；更深层级 **+** 展开。

- 支持伞内**钱包 / UID 定位**（行为同返佣树：展开路径、翻页、高亮）。

- **倒挂检测**：若任一点 `子比例 ≥ 父比例`，节点红色高亮，顶部横幅显示倒挂数量，提供「点击立即展开倒挂位置」。

### 2\.3\.4 提交前校验（必须全部通过）

| 校验项 | 规则 |
|---|---|
| 主体有效 | 待迁移用户可识别 |
| 目标上级有效 | 目标合伙人存在且可挂接 |
| 迁移比例 ≤ 目标上级比例 | 保证迁入后仍有级差空间 |
| 迁移比例 ≤ 运营上限 | 超过须走其他特批流程（本页不允许直接提交） |
| 禁止挂在自己或自己的下级之下 | 防循环 |
| 代理用户整伞级差 | 应用所有预览中的比例覆盖后，全树严格级差 |
| 深层历史倒挂 | 与本次迁移比例无关的既有倒挂，须在树内先修正，倒挂计数为 0 才可提交 |

树内非根节点修改会收集为 **倒挂修正清单**（钱包、原比例 → 新比例），写入审批详情。

### 2\.3\.5 提交与审批

预览合法后，底部展示：

| 元素 | 规则 |
|---|---|
| 审核备注说明 | 提交前填写 |
| 图片附件 | 最多 **4 张** |
| 【提交风控审核】 | 仅当：主体已识别、目标与比例已填、**倒挂错误数为 0** 时可点 |

确认弹窗汇总：待迁移用户、目标上级、迁移后比例、倒挂修正列表、备注、附件文件名。

提交后生成审批单「**返佣关系迁移**」，审批流：**市场运营提交 → 风控审核 → 通过即生效**（无需老板审批）。

---

## 2\.4 合伙人审核

**访问路径**：侧栏 → 「合伙人审核」（页内标题可为「合伙人配置审批」）

**页面作用**：处理三类合伙人配置变更审批；风控、老板切换角色 Tab 处理待办。

### 2\.4\.1 审批类型与流程

| 审批类型 | 触发场景 | 审批流 |
|---|---|---|
| 一级合伙人绑定（超上限） | 新增一级合伙人且比例 &gt; 运营上限 | 市场运营提交 → **风控** → **老板**（风控通过后同步 Lark） |
| 返佣比例调整（超出上限） | 返佣树提交且单项新比例 &gt; 运营上限 | 同上 |
| 返佣关系迁移 | 迁移返佣关系页提交 | 市场运营提交 → **风控** → 通过即生效 |

初始状态均为「待风控」。合伙人三类审批**无**市场运营交叉审核步骤。

### 2\.4\.2 列表筛选

| 筛选项 | 选项 |
|---|---|
| 视图 | 待我审批 / 全部审批 |
| 状态 | 全部、待交叉审核、待风控、待老板、已通过、已驳回 |
| 审批类型 | 一级合伙人绑定、返佣比例调整、返佣关系迁移 |
| UID / 钱包地址 | 模糊匹配主体用户 |
| 审批单号 | APR… |

**角色 Tab**：风控、老板（根据当前 Tab 过滤「待我审批」）。

**列表列**：审批单号、类型、申请人、申请时间、钱包地址、UID、摘要、状态、【查看】/【审批】。

列表分页 10 条/页。

### 2\.4\.3 审批详情

**通用区块**：审批单号、类型、申请人、申请时间、摘要、申请备注、审批时间线、审批进度图、 Lark 卡片（已同步时）、审批操作（通过 / 驳回；驳回须填意见）。

**一级合伙人绑定（超上限）详情**：

- UID、钱包、申请返佣比例、运营配置上限、是否超上限。

- **图片附件**：缩略图网格，点击查看大图。

**返佣比例调整（超出上限）详情**：

- UID、钱包、原/新返佣比例、运营上限、是否超上限、**修改原因备注**。

- **图片附件**（与绑定、迁移一致展示）。

**返佣关系迁移详情**：

- 待迁移用户、UID、类型（普通用户 / 代理用户）。

- 迁移到上级合伙人、迁移后返佣比例。

- **下级返佣修改**（倒挂修正列表）。

- **图片附件**。

---

## 2\.5 操作记录

**访问路径**：侧栏 → 「操作记录」（位于佣金对账与发放之下）

**数据来源**：合伙人三类审批的时间线事件聚合展示。

### 筛选

| 筛选项 | 说明 |
|---|---|
| 操作人 | 后台账号（子串匹配） |
| 操作类型 | 全部、提交审批、返佣关系迁移、一级合伙人绑定、返佣比例调整、交叉审核通过、风控审核通过、老板审批通过、审批驳回 |
| 审批单号 | APR… |
| 审批类型 | 一级合伙人绑定、返佣比例调整、返佣关系迁移 |

### 列表列

时间、操作人、操作类型（标签样式）、审批单号、审批类型、摘要、备注。

分页 10 条/页。

---

## 2\.6 佣金对账与发放页（风控用）

**访问路径**：侧栏 → 「佣金对账与发放」

**页面作用**：每日结算批次的审查、实发/补发人工调整、最终放款或拒绝。

### 2\.6\.1 结算批次列表

**筛选**：结算日期（日期选择器）、批次状态（全部 / 等待对账 / 已拒绝）；查询 / 重置。

| 列名 | 数据定义 |
|---|---|
| 结算日期 | 结算周期对应日期 |
| 返佣总成交额 | 当日参与返佣计算的总成交额 |
| 补发修正后发放金额 | 该**发放日**执行的修正返佣补发合计 |
| 今日待发放总佣金 | 当日批次中无需「待修正」的实发佣金合计 |
| 合计待发放 | 补发修正 + 今日待发放 |
| 状态 | `等待对账` / `已拒绝` |
| 操作 | 【查看详情】；【查看原因】（已拒绝批次） |

分页 10 条/页。进入本页默认展示批次列表视图。

### 2\.6\.2 审核详情页

**顶部**：

- 【← 返回】批次列表。

- 【下载对账单】：导出该批次四表 CSV 包（Platform\_Summary / Affiliate\_Map / Rebate\_Split\_Detail / System\_Proposed\_Payout）。

- 【拒绝放款】、【审核通过并放款】。

**横幅**：

- 已拒绝：红色横幅「该批次已被拒绝放款」。

- 异常说明：批次内异常为**返佣分支**暂停，非整棵一级树；补发金额已计入列表「补发修正后发放金额」。

**Tab：批次明细**

**筛选**：合伙人（钱包/UID）、层级 L1–L5、待修正返佣（是/否）、已修改实发（是/否）；重置；【批量修改实发】。

| 列名 | 说明 |
|---|---|
| 合伙人 | 钱包、UID |
| 层级 | Lx |
| 返佣比例 | |
| 上级 | 上级钱包；一级显示「一级」 |
| 待修正 | 是否待修正返佣后计算 |
| 成交额 | |
| 原始佣金 | 待修正时显示「待修正返佣后计算」及原应结日 |
| 实发 | 人工调整后的实发；低于原始时标「已调减」 |
| 操作 | 【修改】；待修正行不可改实发 |

**Tab：修正返佣补发**

**筛选**：合伙人、**原应结日**（日期）；重置；【批量修改补发】。

| 列名 | 说明 |
|---|---|
| 合伙人 | 钱包、UID |
| 原应结日 | 异常暂停前原本应结算的日期 |
| 原始返佣 | 按修正后比例重算前的参考值 |
| （实际）补发佣金 | 可人工调减；低于原始时标「已调减」 |
| 操作 | 【修改】 |

两个 Tab 内表格均分页 10 条/页。

### 2\.6\.3 人工调整规则

| 场景 | 规则 |
|---|---|
| 修改实发 | 实发不得高于**原始佣金**；`待修正返佣后计算` 行禁止修改 |
| 修改补发 | 补发不得高于**原始返佣** |
| 单条修改 | 弹窗输入金额保存 |
| 批量修改 | 独立批量页：支持手工逐行或上传 CSV/TXT/XLSX |

**批量格式**：

- 实发：`钱包或UID,实发金额`

- 补发：`钱包或UID,原应结日,补发金额`

批量提交后展示逐行成功/失败报告。

### 2\.6\.4 放款与拒绝

- **审核通过并放款**：确认后按对账后金额执行发放，批次状态更新（原型为演示提示）。

- **拒绝放款**：必填拒绝原因；批次标记已拒绝，返回列表。

### 2\.6\.5 对账单数据逻辑定义（.csv）

#### 导出结算数据表

1. **表 1：平台总交易对账表 (Platform\_Summary\.csv)**

*宏观对账：验证「大池子」是否算错。*

- **结算日期**：YYYY\-MM\-DD

- **计算周期**：例如 2026\-05\-22 00:00:00 至 23:59:59

- **平台总成交额**

- **全站可参与返佣的手续费 (Net Fee)**：剔除做市商后的净额

- **全站返佣总预算 (Total Payout)**：所有代理应得金额总和

- **返佣/手续费占比**：`Total Payout / Net Fee`（用于快速观察整体返佣成本是否在预警线内）

2. **表 2：返佣关系映射表 (Affiliate\_Map\.csv)**

*关系字典：风控用于核对某个代理的层级和比例设置是否合规。*

- **用户地址**

- **直接上级地址 (Parent)**

- **顶级一级代理地址 (Root)**：该分支的最顶头大佬

- **用户当前等级**：1=一级，2=二级…以此类推

- **用户返佣比例**：例如 50%

- **上级返佣比例**：例如 60%

- **级差空间**：10%（即上级从该用户身上赚取的比例）

- **完整路径 (Full Path)**：`Root_Addr / Level2_Addr / Level3_Addr / Current_Addr`

3. **表 3：分账明细流水表 (Rebate\_Split\_Detail\.csv)**

*对账核心：记录「谁因为谁产生了多少钱」，并标记链路。*

- **计算周期/日期**

- **\[源\] 产生交易用户地址**、期间成交额、产生手续费 (Net Fee)、上级地址、顶级一级代理

- **\[分\] 佣金获得者地址**、分账角色类型 (`SELF` / `DIRECT_GAP` / `INDIRECT_GAP`)、分账级差比例、最终分账金额 (USDT)

4. **表 4：系统预发放汇总表 (System\_Proposed\_Payout\.csv)**

*发放依据：后台列表的数据源。*

- **结算日期**、代理地址、所属顶级代理 (Root)、代理等级

- **待结算返佣总额**、**人工调减金额**、**最终实发金额**

#### **风控实操案例（审计流程）**

**第一步：总量对账（宏观核减）** — 打开表 1，校验净手续费与「平台留存 + 返佣总预算」守恒。

**第二步：系统聚合校验** — 对表 3 做枢纽分析，按获得者地址汇总分账金额。

**第三步：差异比对** — 表 4 与第二步结果 VLOOKUP，差异列须全为 0。

**第四步：穿透抽样** — 抽检代理，核对表 3 分账比例与表 2 级差关系一致。

---

# 用户合伙人管理页

**产品原型链接**：https://dashuaibizzy\.github\.io/prototype/perp\_dex/代理中心\.html

本页面面向所有已注册合伙人，是查看业绩、管理下级、追踪佣金的核心操作页。对外文案统一使用「合伙人」，不使用「代理」。

## 3\.1 团队数据总览页

默认登录后展示。

### 3\.1\.1 全局时间过滤器

- 位置：页面右上。

- 选项：`1D` / `1W` / `1M` / `3M`，选中项黑底白字。

- 作用：影响下方所有卡片和表格中的数据范围。

### 3\.1\.2 核心指标卡片（4张）

|卡片|数据定义|
|---|---|
|**团队总交易额**|自己 + 全部下级代理 + 全部直客在选定时间内的总成交额。|
|**总返佣收入 (本期预估)**|本期预计可获得的返佣总和。下方拆分为三部分：自返佣、直客贡献（从直客身上拿到的全额比例）、级差（从直属下级代理处赚到的差额）。|
|**团队净入金**|整条线的净充值（充值 − 提现）。|
|**活跃下级代理**|活跃下级代理数 / 总代理下级数。活跃 = 选定周期内交易额 &gt; 0。查询时实时计算，不落库。|

### 3\.1\.3 异常警告横幅

位于卡片下方、表格上方。当代理**自身**或**其任何直属下级**存在配置异常时显示。

- **文案**：「团队全链路配置异常提醒」；展示异常暂停交易额、异常返佣线条数；说明结算状态 `待修正返佣后计算` 及恢复后下一日 00:00 重算规则。

- **操作按钮**：

    - **【导出异常明细】**：导出异常下级清单。

    - **【查看链路规则】**：可打开帮助说明。

### 3\.1\.4 数据明细表格（Tab 切换）

**Tab：直属下级合伙人明细**（默认打开）

表格上方右侧有：

- **状态筛选**：`全部正常` / `异常预警` 切换标签。

- **搜索框**：可搜索地址或备注。

- **【+ 新增下级代理】按钮**：打开新增弹窗。

|列名|数据定义|
|---|---|
|**加入时间**|该下级代理绑定的时间。|
|**下级代理 (备注)**|钱包地址（粗体）+ 备注名。若该下级比例异常，备注区显示红色警告文字。|
|**设置比例**|你给该下级设置的返佣比例。异常时红色加粗。|
|**我的级差**|`我的比例 - 他的比例`。异常时红色负值。|
|**贡献级差收入**|本期该下级为我带来的级差佣金总和。异常时 `-- 暂停结算`。|
|**总交易额**|该下级整条线的总成交额。|
|**总净入金**|该下级整条线的净充值。|
|**用户规模**|`总注册 / 活跃`。|
|**操作**|**【查看团队】**：进入下级团队穿透详情。异常行置灰。<br>**【调整】**：编辑下级返佣比例（只能下调）。自身异常时不可调整下级。|

**Tab：自邀直客明细**

|列名|数据定义|
|---|---|
|**注册时间**|直客注册时间。|
|**直客钱包/UID**|标识。|
|**累计交易额**|本期交易额。|
|**累计手续费**|本期产生的手续费。|
|**为我贡献返佣**|从该直客身上获得的全额佣金。|
|**状态**|显示「交易中」或其他。|

### 3\.1\.5 新增下级代理弹窗

|元素|说明|
|---|---|
|**下级钱包地址/UID 输入框**|必填。|
|**配置返佣比例滑块**|范围：0% 至 **当前登录代理的自身比例**（动态最大值）。|
|**备注名称**|选填。|
|**【确认绑定下级】按钮**|校验后绑定生效。若代理自身处于异常状态，禁止新增。|

**业务规则**：

- 新增下级时，系统检查代理自身是否异常，若异常则禁止新增，提示「请先修复自身比例合规」。

---

## 3\.2 下级团队穿透详情页

从「直属下级代理明细」点击【查看团队】进入，展示特定下级代理的整条业务线数据。

**顶部**：

- **【← 返回】按钮**：回到数据总览页。

- **标题**：`{代理名} 的团队业绩`，副标题显示 ID 和加入时间。

- **时间筛选器**：与总览页相同。

**数据看板（4张卡片）**：

- 本期伞下总交易额

- 本期团队净入金

- 活跃/总人数

- **本期为我贡献总收入**（大号蓝字）：即该代理贡献给「我」的级差总和。

**三种收益拆解（3个小方块）**：

- 自返佣贡献、直客贡献、下级级差贡献。

**贡献明细清单表**：展示所有为「被下钻代理」直接贡献交易的主体。

|列名|数据定义|
|---|---|
|**钱包地址**|贡献者的地址。|
|**他的上级**|他的直属上级。|
|**期间交易额**|他本期交易额。|
|**你的手续费返佣**|你（我）从该笔交易中获得的返佣金额。|
|**期间净充值**|他的净充值。|

---

## 3\.3 佣金结算记录页

**访问路径**：侧栏 → 「佣金结算记录」

**页面作用**：让代理清楚知道自己的钱什么时候结、结了多少。

**状态卡片（3张）**：

- 待结算佣金（今日预估）

- 已结算总额（累计）

- 昨日发放佣金（Success 状态）

**每日结算流水列表**：

|列名|数据定义|
|---|---|
|**结算日期**||
|**有效交易额**|参与当日返佣计算的交易额。|
|**产生总手续费**|当日产生的手续费。|
|**应结佣金 (Rebate)**|当日应得的返佣金额。|
|**结算状态**|`已发放` / `待结算` / `待修正返佣后计算` / `补结算`。|
