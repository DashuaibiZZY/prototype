# 邀请合伙人无限层返佣系统

# 业务定义

## 1\.1 核心业务模型

**模型定义：无限级差佣金制**

- 平台为每个一级合伙人设定一个返佣比例上限（运营可在权限后台配置，当前原型演示为 **80%**），该比例即为此合伙人整条业务线的「总佣金包」上限参考。

- 合伙人可以无限层级地发展下级合伙人（平台对**新增发展**设有最多 **10 层**合伙人层级限制，见 §1.4），每一级合伙人的返佣比例由其直属上级设定，且**不得超过**上级自身的比例。

- **级差与合规**：上下级比例**允许相等**（级差 = 0，该上级在该笔交易上级差为 0，属正常配置）。仅当 `下级返佣比例 > 上级返佣比例` 时判定为**倒挂异常**（见 §1.4）。

- 当终端用户（直客或合伙人自营）产生交易手续费时，系统沿邀请链路向上逐级计算级差佣金，直到一级合伙人为止。

**角色定义**

|角色|账号类型|交易时身份|是否参与返佣分配|
|---|---|---|---|
|一级合伙人|Agent|渠道/交易者|是，享受自返佣，并从下级合伙人的级差中获利|
|N级合伙人 \(N≥2\)|Agent|渠道/交易者|是，享受自返佣，并从直属下级合伙人的级差中获利|
|直客|Client|纯交易者|是，为上级合伙人贡献级差的「燃料」，自己不拿返佣|

**核心原则：合伙人自营也参与返佣**

- 合伙人用自己的合伙人账号交易，自己拿走自己返佣比例对应的佣金（自返佣）。

- 同时，他的所有上级依然按级差规则分钱。

## 1\.2 返佣计算规则

### 规则一：自返佣

- 触发条件：代理使用自己的代理账号进行交易并产生手续费。

- 计算：`该代理的返佣比例 × 该笔手续费`

- 拿到钱的人：该代理本人。

### 规则二：直客级差

- 触发条件：直客进行交易并产生手续费。

- 计算：

    - 直客的直属上级代理：拿走自己**全额**返佣比例对应的佣金。

    - 该上级的每一位上级：拿走 `(自己的返佣比例 - 直属下级代理的返佣比例)` 的级差部分。

- 如果上级与直属下级的比例相同，则该上级在这笔直客交易上级差为 0。

### 规则三：代理自营级差

- 触发条件：某代理用自己代理号交易产生手续费。

- 该代理自己先拿走自返佣（规则一）。

- 他的所有上级，按规则二的方式，从他身上计算级差，即 `(上级比例 - 该代理比例) × 该笔手续费`。

### **公式总结：**

- 对任意一笔交易：

    1. 确定交易发起人 `X`（可能是代理或直客）。

    2. 如果 `X` 是代理，`X` 获得 `比例_X × 手续费`。

    3. 沿着 `X` 的上级链路向上遍历，当前上级 `P`：

        - 如果 `P` 是 `X` 的直属上级，`P` 获得 `(比例_P - 比例_X) × 手续费`

        - 否则，`P` 获得 `(比例_P - 比例_直属下级) × 手续费`

    4. 直到一级代理停止。

## 1\.3 返佣结算流程

**整体流程：日结制**

1. **交易数据采集**：系统实时记录每一笔交易的手续费。

2. **每日结算触发**：每日 00:00 \(UTC\+8\)，系统对前一日 00:00:00 至 23:59:59 的所有交易进行结算。

3. **数据同步**：生成用于审查的对账 CSV 包（表 1–4，及建议一并导出的一级比例快照表 2a），进入风控审批队列。

4. **人工审查**：风控或运营人员登录后台「佣金对账与发放」页面，进行四步审计：

    - **总量对账**：用 Platform\_Summary 核对平台总手续费与总返佣预算。

    - **聚合校验**：用 Rebate\_Split\_Detail 的流水表通过 Excel 枢纽分析，反向汇总每个代理应得总金额。

    - **差异比对**：将汇总结果与 System\_Proposed\_Payout 做 VLOOKUP 比对，排查是否存在非零差异。

    - **穿透抽样**：随机抽检一个代理，验证其分账流水是否与 Affiliate\_Map 中的比例关系吻合。

5. **审批执行**：

    - **通过**：风控点击「审核通过并放款」，系统将最终实发金额执行发放。

    - **拒绝**：风控填写拒绝原因，该批次标记为「已拒绝」，打回重算。

6. **数据归档**：所有 CSV 文件和审批记录存档备查。

## 1\.4 返佣比例调整、关系迁移与异常配置保护

### 比例倒挂（级差异常）

- **比例调整触发检查**：当任何代理的返佣比例被调低时（平台调一级、上级调下级、或在返佣树内批量修改），系统必须实时检查该代理名下**所有直属下级**的返佣比例。

- **异常判定标准**：若存在 `下级返佣比例 > 上级返佣比例`，则判定为**配置异常（倒挂）**。

- **允许级差为 0**：`下级返佣比例 = 上级返佣比例` 时**不算异常**，仅该上级在相关交易上的级差为 0，结算与对账正常进行。

- **异常影响范围**：以发生倒挂的**该返佣分支**为起点（该上下级节点及其下级），从异常当下起暂停该分支的返佣结算。**并非**整棵一级合伙人树下所有分支停结；同一一级下其他正常分支继续结算。

- **异常状态标记**：该返佣分支上的合伙人标记为 `待修正返佣后计算`（一级根节点若仅部分分支异常，可显示 `部分分支异常`）。异常期间该分支交易额计入「异常暂停交易额」；「异常返佣线」为倒挂的上下级地址对数量（仅统计「下级 &gt; 上级」的对数）。

- **暂停期间不展示/不计算应发返佣**：比例倒挂导致暂停结算时，系统**尚未完成应发返佣计算**（因比例关系无效）；运营侧仅展示 **暂停结算交易额**、**暂停结算手续费**，修正比例并恢复结算后再按新比例重算应发返佣。

### 修正与恢复

- 运营需进入「合伙人管理」或「返佣关系树」，找到比例倒挂的下级，**将其比例调低至 ≤ 上级比例**（等于上级即可解除倒挂）。

- 一旦该合伙人名下所有直属下级比例均 **≤** 自身比例，系统自动解除异常标记，**立即恢复**结算资格。

- 恢复后，对异常期间未结算的交易用**修正后的新比例**重算佣金。发放方式分两类（见 §1.4 补发规则与 §2.6）：

    - 若原应结日所属批次**尚未放款**（`等待对账` 或 `已拒绝`）：不进入「修正返佣补发」；修正后在该批次重算或随新批次正常实发。

    - 若原应结日所属批次**已审核通过并放款**：未实发部分进入「修正返佣补发」，按**原应结日**挂账，在后续某发放日补发。

### 运营权限上限与审批

- 每位市场运营在权限配置中设有 **一级合伙人返佣比例上限（%）**（原型演示为 **80%**），页面顶栏展示「权限上限: XX%」。

- **≤ 运营上限** 的比例调整：在返佣树内提交后**立即生效**（无需审批）。

- **&gt; 运营上限** 的一级绑定或比例调整：须提交审批，流程为 **市场运营提交 → 风控审核 → 老板审批**（风控通过后同步 Lark，老板可在 Lark 或后台通过）。

- **同一批提交中若混合「权限内」与「超上限」修改**：仅超上限项进入审批；权限内修改**自动放弃**（输入框回滚），须先处理超上限申请。

### 返佣关系迁移

- 运营可将某用户（普通用户或代理用户）及其伞下关系整体迁移到新的上级合伙人之下，并重新设定迁移主体的返佣比例。

- 迁移后须满足：迁移比例 ≤ 目标上级比例、≤ 运营上限、且迁移后整伞内任意父子节点满足「下级 ≤ 上级」（允许级差为 0；禁止下级 &gt; 上级）。

- 若迁移主体为代理用户，其原有下级树中若存在**深层倒挂**（下级 &gt; 上级，与本次迁移比例无关的历史问题），须在预览树中先修正再允许提交。

- 迁移申请提交后进入 **风控审核**（无需老板审批）；通过后执行关系变更。

- **迁移后的数据统计与返佣归属**见 §1.4「迁移后的返佣计算与业绩统计」。

### 合伙人层级上限（10 层）与迁移

- 平台对合伙人账号设置**最多 10 层**合伙人层级（L1 为一级合伙人，向下至多 10 层合伙人节点）。

- **用户端（合伙人中心）**：合伙人**新增下级合伙人**时，若会导致链路深度超过 10 层，系统**禁止绑定**新下级。该限制约束的是**新增发展**，不自动裁切已存在的深层节点。

- **迁移返佣关系**：迁移可能使某条业务链在系统内的**实际深度超过 10 层**（例如将较深整伞挂到较浅的上级之下）。迁移完成后：

    - **已存在的深层合伙人节点及其关系保留**，返佣与结算按新关系继续计算；

    - **不因迁移而作废 10 层限制**——限制仍作用于**迁移之后的新增下级**；处于第 10 层及更深处的合伙人，在合伙人中心**不可再发展新的下级合伙人**（与「用户层不允许延续新的下级」一致）。

    - 直客绑定、交易、返佣计算不受「10 层」约束（直客不计入合伙人层数）。

### 迁移后的返佣计算与业绩统计

迁移风控审核通过并生效后，返佣计算**立即**按**新的上级关系**与**新的返佣比例**执行。

**统一规则（不论迁移前正常或异常）**：

| 维度 | 规则 |
|---|---|
| 原上级 | 迁移生效后，该伞**不再累计**新的交易额、手续费、返佣贡献；历史已归属数据保留 |
| 新上级 | 迁移生效后，该伞的业绩与返佣贡献从 **0 重新累计**（周期统计不继承原上级下的数字） |
| 返佣计算 | 生效后按新关系与新比例统计、分账 |

**异常伞迁移的补充**：

- 若迁移前存在倒挂 / `待修正返佣后计算`，迁移至新上级后须在预览或返佣树内**修正比例**。

- 异常期间暂停的交易额、手续费及应计返佣，在修正完成并恢复结算后，**归属到新上级**维度统计与发放（不再挂原上级）。

- 若异常期间涉及**已放款批次**的欠发部分，按 §1.4 补发规则以**原应结日**进入「修正返佣补发」；主体已在新上级伞下。

**说明**：普通用户迁移仅变更邀请关系；代理用户为**整伞迁移**，伞内全部合伙人节点与直客一并变更上级。

## 1\.5 其他补充

### **活跃用户 / 活跃代理定义**

- 定义为「所选时间周期内，交易额 &gt; 0 的用户或代理」。

- 此指标为**查询时实时计算**，不需要数据落库，不需要维护每日活跃状态表。

- 举例：选择统计周期为「1W」，则统计近 7 天内有过至少一笔交易（交易额 &gt; 0）的用户/代理数量。

- **用户合伙人中心「用户规模」展示口径**：主行展示**交易用户**数（周期内交易额 &gt; 0）；次行灰色小字展示**总用户**数（注册或归属伞内总人数，含未交易者）。

### **用户侧佣金与状态展示规则（合伙人中心）**

以下规则仅描述**合伙人登录后**在「佣金结算与发放」及「合伙人管理」中的展示逻辑，与后台批次状态、风控操作对应关系如下：

| 后台 / 系统事实 | 用户侧展示 |
|---|---|
| 当日批次**等待对账**，佣金尚未放款 | **待结算** |
| 当日批次**已审核通过并放款** | **已结算** |
| 当日批次**已拒绝**（打回重算） | 仍展示 **待结算**（不向用户暴露「已拒绝」） |
| 存在**返佣树异常**或**直属下级比例倒挂**，该日相关交易额暂停计入可发返佣 | **返佣异常停止结算**；**交易额有值，返佣金额为 0** |
| 风控对某合伙人**人工调减实发**后 | 「每日结算流水」中**返佣金额**按**修正后实发**展示；「合伙人管理」中**总返佣收入（本期预估）**等同口径，按修正后累计展示 |
| 存在**违规扣减** | 返佣金额主行展示实发/应发净值；次行红色小字 **违规 −$XX** 展示扣减额 |

**返佣异常停止结算**与后台「待修正返佣后计算」的对应关系：

- **直属下级比例倒挂**（仅一层：`直属下级比例 &gt; 我`）：影响该直属下级及其伞；用户在该下级行看到倒挂预警，贡献级差收入暂停。
- **团队返佣树异常**（深层链路中存在 `下级 &gt; 上级`，但不一定是我与直属下级之间）：用户自身或下级行展示异常条数、暂停结算交易额；弹窗可查看每条异常返佣线及**该线停止结算交易额**。

### **净收入（后台指标）**

- 对任意查询根节点（某一级或下级合伙人）：`净收入 = 该伞下净手续费 − 由该伞下交易触发的全部返佣支出`。

- 「全部返佣」包含分给伞内所有合伙人，以及分给伞外上级合伙人的级差（因这些级差均由伞下交易手续费产生）。

- 一级合伙人视角即整条业务线平台留存；下级合伙人视角为「以该合伙人为根的子树」对平台整体的净贡献。

---

# 后台管理：合伙人管理及结算对账

本后台面向两类内部人员：

- **运营/BD 同学**：负责配置和管理一级合伙人、调整返佣树比例、发起关系迁移（仅可见自己负责的数据）。

- **超级管理员 / 风控**：负责维护后台人员与页面权限（含一级合伙人返佣比例上限，见《[后台权限与角色管理](后台权限与角色管理.md)》），执行合伙人相关审批，以及每日佣金对账与发放。

## 侧栏结构

后台采用 **ForX Admin 统一侧栏**（见《[后台权限与角色管理](后台权限与角色管理.md)》§3）。

| 侧栏入口 | 页面 | 对应本文章节 |
|---|---|---|
| 合伙人管理 | 一级合伙人列表、详情、团队穿透、返佣树 | §2.1、§2.2 |
| 迁移返佣关系 | 整伞关系迁移与预览、倒挂修正 | §2.3 |
| 合伙人审核 | 超上限绑定、超上限比例调整、关系迁移审批 | §2.4 |
| 佣金对账与发放 | 每日佣金审计、实发/补发调整与放款 | §2.6 |
| 操作记录 | 合伙人相关审批操作审计日志 | §2.5 |

**产品原型**：[代理中心后台](https://dashuaibizzy.github.io/prototype/perp_dex/admin/代理中心后台.html)

**权限**（与权限后台联动，原型全量展示）：

| Tab | 读 | 写 |
|---|---|---|
| 一级合伙人管理 | 查看合伙人与团队数据（运营仅看自己配置的） | 绑定一级合伙人；在返佣树调整比例；发起迁移返佣关系 |
| 合伙人审核 | 查看审批单 | 风控/老板在对应角色下审批（运营提交） |
| 佣金对账与发放 | 查看对账数据 | 执行对账、调减实发/补发、放款或拒绝 |

运营账号及返佣配置上限已合并至「权限与用户」模块。详见《[后台权限与角色管理](后台权限与角色管理.md)》§4.1、§7。

## 通用交互约定

### 统计周期

列表页、详情页与团队穿透页均提供统计周期切换：**1D / 1W / 1M / 3M / ALL**（默认 ALL）。切换后，团队总交易额、返佣、净入金、用户规模等周期敏感指标同步刷新。列表与详情打开时默认继承列表当前周期。

**说明**：合伙人**列表**仍展示手续费、净收入等运营对账向列；**详情 / 团队穿透**页与用户 Web「合伙人管理」对齐，指标区为四卡（不含手续费、净收入独立卡），见 §2.1.4。

### 分页

合伙人列表、详情子表、返佣树一级支路、迁移预览树、结算批次与明细、操作记录、审批列表等长列表统一 **每页 10 条**，提供页码切换（含省略号大页数样式）。

### 地址与 UID 复制

钱包地址、UID 在列表、详情、树节点、结算、审批详情等处以缩写展示，并提供一键复制完整值，复制成功有短暂提示。

### 页面路由（Hash）

支持浏览器 Hash 直达：`#agent`（合伙人列表）、`#partner-detail`（合伙人详情）、`#partner-detail-drill`（团队穿透）、`#rebate-tree`（返佣关系树）、`#migrate`（迁移返佣关系）、`#approval`（合伙人审核）、`#approval-migrate`（合伙人审核 · 深链至迁移类审批）、`#settlement`（佣金对账）、`#logs`（操作记录）。侧栏点击与页内跳转同步更新 Hash；进入返佣树时保留 `#rebate-tree`；团队穿透页保留 `#partner-detail-drill`。

---

## 2\.1 合伙人管理页

**访问路径**：侧边栏 → 「合伙人管理」

**页面作用**：查询并管理本人负责的一级合伙人；进入详情或团队穿透查看向下伞数据；进入返佣树修改比例；新增一级合伙人。

页面截图：[**后台 · 合伙人列表与筛选**](https://raw.githubusercontent.com/DashuaibiZZY/prototype/doc-assets/document/assets/%E5%90%88%E4%BC%99%E4%BA%BA/admin-partner-list.png)

**列表可见范围（运营账号）**：

- 当前登录运营**仅能查看**在权限后台为其配置的 **一级合伙人**（非全站一级伞、非其他运营负责的一级）。

- 从列表【详情】或下级【查看团队】进入后，可继续查看该一级合伙人 **向下所有层级** 的代理与直客数据，直至无下级合伙人（见 §2.1.5）。

- **原型演示**为覆盖多种异常场景，列表可能包含非一级样例行（如三级 `p_n3`、四级 `p_a4`）；**生产环境**列表仅展示上述一级合伙人。

**原型自测参考**（演示数据，非产品规则）：

| 样例 | 用途 |
|---|---|
| `p_n1` · 正常结算·一级 | 正常伞、多级下级穿透 |
| `p_a1` · 部分分支异常·一级 | 异常横幅、§2.1.7 弹窗、返佣树暂停结算区块 |
| `p_a4`（经 `h_a3` 穿透） | 直属比例倒挂（62% &gt; 上级 55%） |

**页头（全局）**：顶栏右侧固定展示 **权限上限: XX%**（与当前登录运营在权限后台的配置一致；原型演示为 80%）。列表区不再单独重复该提示。

### 2\.1\.1 查询与筛选

| 筛选项 | 说明 |
|---|---|
| 钱包 / UID / 备注 | 在**本人负责的一级伞范围内**模糊查询任意层级合伙人（命中后展示对应记录）；搜索框 placeholder 为「本人负责一级伞内搜索」 |
| 结算状态 | 全部 / 正常 / 异常（含分支异常、待修正返佣后计算等非正常状态） |
| 统计周期 | 1D / 1W / 1M / 3M / ALL，影响列表金额列 |

**口径说明（列表区脚注）**：

- 团队指标均为该用户**向下整伞**汇总。

- 净收入 = 伞下净手续费 − 由伞下交易触发的**全部返佣**（含向上级级差）。

- 异常仅暂停**相关返佣分支**，非整棵一级树。

- 系统每 **5 分钟**校验级差关系。

### 2\.1\.2 查询列表

默认展示当前运营配置的 **一级合伙人**；搜索可在本人负责伞内定位到任意层级记录。

| 列名 | 说明 |
|---|---|
| 合伙人 | 钱包、UID（可复制）；备注名；点击进入详情 |
| 层级 | 平台层级 **Lx**（合伙人链路最多 **10 层**，见 §1.4）；展示 **直属**下级合伙人数 |
| 返佣比例 | 当前配置比例 |
| 结算状态 | `正常结算` / `部分分支异常` / `待修正返佣后计算` |
| 异常暂停交易额 | 仅该用户关联异常分支的暂停交易额；无则 `--` |
| 异常线 | 倒挂上下级地址对条数 |
| 团队总交易额 | 当前统计周期内向下伞总成交额；**可排序** |
| 手续费 | 当前周期伞下手续费；**可排序** |
| 返佣 | 当前周期伞下返佣；**可排序** |
| 净收入 | 当前周期净收入；**可排序**；数据不可用时可 `--` |
| 团队净入金 | 向下伞净入金 |
| 用户规模 | 活跃人数 / 总人数（向下伞） |
| 配置运营 | 一级合伙人显示负责运营邮箱；非一级显示 `—` |
| 操作 | 【详情】【返佣树】 |

**排序**：点击「团队总交易额 / 手续费 / 返佣 / 净收入」列头切换升序/降序（默认降序）。

**操作**：【+ 新增一级合伙人】打开绑定弹窗。

### 2\.1\.3 新增一级合伙人（弹窗）

页面截图：[**后台 · 新增一级合伙人弹窗**](https://raw.githubusercontent.com/DashuaibiZZY/prototype/doc-assets/document/assets/%E5%90%88%E4%BC%99%E4%BA%BA/admin-modal-bind-partner.png)

| 字段 | 规则 |
|---|---|
| 钱包 / UID | 必填；纯数字输入视为 UID |
| 返佣比例 (%) | 必填 |
| 申请备注 | **必填**，说明绑定原因、渠道背景 |
| 图片附件 | 选填，最多 **4 张**图片（渠道协议、身份证明等） |

**提交逻辑**：

- 返佣比例 **≤ 运营配置上限**：直接绑定生效（生产环境落库；原型演示为成功提示）。

- 返佣比例 **&gt; 运营配置上限**：生成审批单「**一级合伙人绑定（超上限）**」，进入合伙人审核（风控 + 老板）；附件与备注写入审批详情。

### 2\.1\.4 合伙人详情

**入口**：列表【详情】。

**布局定位**：与用户侧 Web「合伙人管理」（§3.2）**镜像同构**——相同的上级/比例信息条、四张指标卡、团队返佣异常横幅、直属下级 / 自邀直客 Tab 与结算状态筛选。差异在于后台展示**全量敏感信息**（完整钱包、UID、备注等），且均支持 **一键复制**（见上文「地址与 UID 复制」）。

页面截图：[**后台 · 合伙人详情（正常伞）**](https://raw.githubusercontent.com/DashuaibiZZY/prototype/doc-assets/document/assets/%E5%90%88%E4%BC%99%E4%BA%BA/admin-partner-detail-normal.png)

页面截图：[**后台 · 合伙人详情（分支异常）**](https://raw.githubusercontent.com/DashuaibiZZY/prototype/doc-assets/document/assets/%E5%90%88%E4%BC%99%E4%BA%BA/admin-partner-detail-abnormal.png)

**可见范围**：展示当前详情对象在其一级伞下的向下数据。运营从列表进入的均为本人配置的 **一级合伙人**；从下级【查看团队】穿透时，焦点切换为该下级及其向下伞（见 §2.1.5）。

**顶部操作**：

- 【← 返回】回到列表。

- 【查看异常返佣线】：该一级伞存在**系统登记的倒挂异常线**时显示；打开 **§2.1.7 异常返佣线查询弹窗**（非 §2.1.6 团队返佣树弹窗）。

- 【返佣树 / 修改比例】：**右上角固定**；进入返佣关系树，以**当前详情合伙人**为焦点。

**团队返佣异常横幅**（文案风格对齐 §3.2.4）：

- 琥珀色横幅，展示异常返佣线条数、暂停结算交易额合计。

- 引导在下级表中查看异常明细并与下级沟通调整比例。

**统计周期**：1D / 1W / 1M / 3M / ALL（默认继承列表当前周期）。

**上级合伙人 / 返佣比例**（布局同 §3.2.2）：

| 区域 | 说明 |
|---|---|
| 上级合伙人 | 当前详情对象的直属上级；一级合伙人展示 **一级代理**；非一级展示上级钱包、UID（均可复制） |
| 返佣比例 | 当前对象生效的返佣比例 |

**指标卡片**（四卡，同 §3.2.3；**不含**手续费、净收入独立卡）：

| 卡片 | 说明 |
|---|---|
| 团队总交易额 | 该对象向下伞成交额（当前周期） |
| 总返佣收入（本期预估） | 拆分：自返佣 / 直客 / 级差 |
| 团队净入金 | 向下伞净入金（含穿透） |
| 用户规模 | 主行：交易用户；次行：总用户 |

**Tab 区**（结构同 §3.2.5，后台操作差异见下）：

- Tab：**直属下级合伙人明细**（默认）、**自邀直客明细**。

- **结算状态筛选**（仅直属下级 Tab）：全部 / 正常 / 返佣比例倒挂（直属下级）/ 团队返佣树异常。

- **搜索**：钱包、UID 或备注；**同一关键词同时过滤**直属下级 Tab 与自邀直客 Tab（切换 Tab 不丢失关键词）。

- **无**【+ 新增下级合伙人】、**无**行内【调整】——下级比例修改统一走【返佣树 / 修改比例】。

#### Tab：直属下级合伙人明细

| 列名 | 说明 |
|---|---|
| 加入时间 | 绑定时间 |
| 下级合伙人（备注） | 钱包、UID（可复制）；备注 |
| 设置比例 | 直属倒挂时红色高亮 |
| 上级级差 | 当前对象比例 − 下级比例；倒挂为负值高亮 |
| 结算状态 | 同 §3.2.5「用户侧结算状态」；**团队返佣树异常**可点击打开 §2.1.6 弹窗 |
| 贡献级差收入 | 倒挂或树异常停结时 **-- 暂停结算** |
| 总交易额 | 该下级向下伞成交额（当前周期） |
| 总净入金 | 该下级向下伞净入金 |
| 用户规模 | 交易用户 / 总用户 |
| 操作 | 【查看团队】→ §2.1.5 团队穿透 |

#### Tab：自邀直客明细

| 列名 | 说明 |
|---|---|
| 注册时间 | |
| 直客钱包 / UID | 均可复制 |
| 累计交易额 | |
| 累计手续费 | |
| 返佣金额 | 列名为「返佣金额」（用户 Web 同列亦称贡献返佣语义） |
| 状态 | 如「交易中」；**无**净入金列（用户 Web 直客表含净入金，见下表差异） |

两 Tab 均分页（10 条/页）。

**佣金结算记录**：**不在详情页展示**。用户每日结算流水见 Web §3.4；后台批次对账、实发调减与放款见 §2.6。

**与用户 Web 差异汇总**：

| 能力 | 用户 Web（§3.2） | 后台（§2.1.4） |
|---|---|---|
| 钱包 / UID | 部分可复制；穿透页脱敏 | 全量展示，均可复制 |
| 直客表净入金列 | 有 | **无**（后台详情/穿透不展示该列） |
| 新增 / 调整下级 | 有 | 无（走返佣树） |
| 佣金日结流水 | 独立页 §3.4 | 不在详情；见 §2.6 |
| 返佣树入口 | 无 | 右上角固定 |
| 一级伞异常快捷入口 | 无 | 详情页【查看异常返佣线】→ §2.1.7 |

---

### 2\.1\.5 团队穿透（查看团队）

**入口**：详情或穿透页内直属下级【查看团队】。

**穿透范围**：在运营可见的一级伞内，支持**无限向下穿透**（下级合伙人的下级仍可【查看团队】），直至无下级合伙人。

页面截图：[**后台 · 团队穿透页**](https://raw.githubusercontent.com/DashuaibiZZY/prototype/doc-assets/document/assets/%E5%90%88%E4%BC%99%E4%BA%BA/admin-partner-drill.png)

**布局定位**：与用户侧 §3.3 **镜像同构**；数据范围为**当前被查看合伙人**的向下伞。

**页头**：

- 【← 返回】：多层穿透时回到上一层团队页；仅穿透一层时回到 §2.1.4 合伙人详情（列表进入的一级或上一级穿透对象）。

- 标题：`{合伙人备注名} 的团队`；副标题含**完整**钱包、UID、加入时间；多层穿透时展示当前层级深度。

- 【返佣树 / 修改比例】：**右上角固定**；焦点为当前穿透中的合伙人。

- 统计周期：同 §2.1.4。

**上级合伙人 / 返佣比例**：同 §2.1.4，但展示对象为**当前被查看的合伙人**（非列表一级合伙人本身）；上级钱包、UID 可复制。

**指标卡片与异常横幅**：同 §2.1.4 四卡 + §3.2.4 风格横幅，口径为当前焦点合伙人向下伞。

**明细表格**：

- Tab、列、结算状态筛选、搜索、分页规则同 §2.1.4。

- **搜索**使用穿透页独立输入框（`drill-sub-search`），与详情页搜索状态分离。

- **无**新增下级、**无**调整操作。

- 仍有【查看团队】（该下级仍有团队时）。

- 钱包、UID、备注**全量展示且可复制**（用户穿透页为脱敏只读，见 §3.3.4）。

**Hash**：`#partner-detail-drill`。

---

### 2\.1\.6 团队返佣树异常明细弹窗

从直属下级行「团队返佣树异常」链接触发；规则同 §3.2.8。

页面截图：[**后台 · 团队返佣树异常明细弹窗**](https://raw.githubusercontent.com/DashuaibiZZY/prototype/doc-assets/document/assets/%E5%90%88%E4%BC%99%E4%BA%BA/admin-modal-team-tree.png)

**后台差异**：弹窗内异常链路上各节点钱包、UID、备注**不脱敏**，均可一键复制；每条异常线独立展示**停止结算交易额**。

穿透页内触发时同样为全量可复制展示（与用户 Web 穿透页脱敏规则不同，见 §3.2.8 末句）。

---

### 2\.1\.7 异常返佣线查询弹窗（详情页）

**入口**：合伙人详情【查看异常返佣线】（仅当该 L1 伞存在系统登记的倒挂异常线时显示按钮）。

**弹窗标题**：异常返佣线查询。

页面截图：[**后台 · 异常返佣线查询弹窗**](https://raw.githubusercontent.com/DashuaibiZZY/prototype/doc-assets/document/assets/%E5%90%88%E4%BC%99%E4%BA%BA/admin-modal-abnormal-list.png)

**内容**：嵌入与 §2.2.4 同款的 **「暂停结算待处理」** 模块——倒挂条数说明、暂停结算交易额/手续费汇总、明细表（上级 / 下级倒挂钱包及比例、暂停结算交易额与手续费）。

**操作**：每行【展开倒挂位置】跳转至返佣关系树，自动展开路径并定位到倒挂下级节点（与迁移预览树内【展开倒挂位置】交互一致）。

**与 §2.1.6 的区别**：

| 入口 | 触发位置 | 数据范围 |
|---|---|---|
| §2.1.7 本弹窗 | 详情页顶部按钮 | 当前 L1 **整伞**内全部倒挂异常线 |
| §2.1.6 团队返佣树弹窗 | 直属下级表「团队返佣树异常」链接 | **该直属下级**伞内异常链路的节点明细 |

返佣关系树页顶部亦展示 §2.2.4 同款「暂停结算待处理」区块（非弹窗），三处 UI 样式一致。

---

## 2\.2 返佣关系树

**入口**：合伙人列表【返佣树】、详情或团队穿透页【返佣树 / 修改比例】、§2.1.7 / §2.2.4【展开倒挂位置】异常定位。

**返回**：页头【← 返回】回到 **当前焦点合伙人的详情页**（`§2.1.4`），**不是**合伙人列表。若自穿透页进入返佣树，返回后亦进入该合伙人详情（不保留穿透栈层级）。

**核心原则**：无论从哪一层合伙人进入，树始终**锚定在该用户所属的一级合伙人根节点（L1 伞）**上展示完整业务线；进入深层的合伙人时，自动**定位并高亮**该节点，而非只展示以该节点为根的子树片段。

页面截图：[**后台 · 返佣关系树**](https://raw.githubusercontent.com/DashuaibiZZY/prototype/doc-assets/document/assets/%E5%90%88%E4%BC%99%E4%BA%BA/admin-rebate-tree.png)

### 2\.2\.1 页面结构

- **标题**：展示焦点合伙人备注 + 所属 L1 备注 +「返佣树」。

- **暂停结算待处理区块**（若该 L1 伞存在倒挂分支）：与「迁移返佣关系」页共用同一套展示样式（见 §2.2.4）。不再使用旧的红色「异常返佣线」简表。

- **待提交修改条**：暂存未提交的比例变更，标注「权限内 · 立即生效」或「超权限 · 提交审批」。

- **操作说明**：上级链固定展示；下级默认只展示直属合伙人，点 **+** 逐层展开；多条修改后统一【提交修改】。

- **定位搜索**：在当前 L1 伞内按钱包 / UID 搜索；自动展开路径、翻页到正确的一级直属支路页、高亮节点并滚动到视区。**不会**因定位而折叠整树为单一路径。

### 2\.2\.2 树形展示规则

**一级入口节点**：L1 根固定在顶部；当焦点为 L1 时标记「当前」。

**上级链**：自焦点向上追溯至 L1，完整展示（无旁支）；链上节点均可编辑比例。

**向下展开**：

- **一级直属支路分页**：L1 的**直接下级**合伙人按支路分页展示（10 条/页），每支路可 **+ / −** 展开更深层级。

- 每个节点展示：层级、钱包、UID、备注、可编辑的返佣比例输入框。

**级差规则**：下级返佣比例**不得大于**直属上级（允许相等，级差为 0）。仅 `下级 &gt; 上级` 为倒挂异常。在输入框暂存变更时，若对含下级的节点**下调**比例，弹出提示列出直属下级当前比例，提醒确认级差影响。

### 2\.2\.3 比例修改与提交

运营在树上修改一个或多个节点比例后，变更进入「待提交修改」列表：

| 变更类型 | 新比例 vs 运营上限 | 提交后行为 |
|---|---|---|
| 权限内 | ≤ 运营上限 | 确认后立即生效 |
| 超权限 | &gt; 运营上限 | 每项生成一条「返佣比例调整（超出上限）」审批单 |

**确认提交弹窗**（点击【提交修改】后）：

页面截图：[**后台 · 返佣比例修改确认弹窗**](https://raw.githubusercontent.com/DashuaibiZZY/prototype/doc-assets/document/assets/%E5%90%88%E4%BC%99%E4%BA%BA/admin-modal-tree-confirm.png)

| 元素 | 规则 |
|---|---|
| 变更清单 | 每条展示钱包、原比例 → 新比例、生效方式标签 |
| 混合批提示 | 若同时存在权限内与超权限项：提示仅超权限项进审批，权限内项将**自动放弃**并回滚输入框 |
| 修改原因备注 | **必填** |
| 图片附件 | 最多 **4 张**图片（PNG 等）；超上限审批时随审批单保存，供风控/老板查看 |

**确认逻辑**：

- 全部为权限内：确认后批量生效；若含下调且该节点有下级，须二次确认级差风险。

- 全部为超权限：仅提交审批，不立即改比例。

- 混合：用户确认后，超权限项提交审批；权限内项放弃并回滚。

- 超权限审批通过后，由后台执行比例更新（与一级绑定超上限审批同理）。

### 2\.2\.4 暂停结算待处理（返佣树 / 迁移返佣关系共用）

当存在返佣倒挂（下级比例 &gt; 上级）且相关分支处于暂停结算时，在**返佣关系树顶部**或**迁移预览区**展示统一的「暂停结算待处理」模块：

**顶部说明**：

- 倒挂条数：仅统计「下级 &gt; 上级」；级差为 0 不计入。

- 文案提示：比例异常期间**尚未计算应发返佣**，仅展示暂停结算的交易额与手续费。

**汇总行**：

- 暂停结算交易额合计

- 暂停结算手续费合计

**明细表列**：

| 列名 | 说明 |
|---|---|
| 上级 | 上级钱包及返佣比例 |
| 下级（倒挂） | 下级钱包及返佣比例（高于上级） |
| 暂停结算交易额 | 该倒挂分支暂停期间累计成交额 |
| 暂停结算手续费 | 该分支暂停期间累计手续费 |
| 操作 | 【展开倒挂位置】：在返佣树中展开路径并定位到该下级节点；迁移预览中在代理链路树内展开并高亮对应节点 |

**说明**：

- **不提供「待发放返佣」列**：暂停期间返佣未计算，展示应发金额不合理。

- 返佣树场景：修正下级比例后恢复结算，系统按修正后比例重算应发返佣。

- 迁移场景：副标题说明迁移并修正后，暂停数据归属新上级统计；恢复结算后按新比例计算返佣。

---

## 2\.3 迁移返佣关系

**访问路径**：侧栏 → 「迁移返佣关系」

**页面作用**：将普通用户或代理用户（及其伞下关系）整体迁移到新的上级合伙人之下，并设定迁移后的返佣比例；提交前须在预览中完成倒挂校验与修正。

页面截图：[**后台 · 迁移返佣关系录入区**](https://raw.githubusercontent.com/DashuaibiZZY/prototype/doc-assets/document/assets/%E5%90%88%E4%BC%99%E4%BA%BA/admin-migrate-form.png)

### 2\.3\.1 录入区

| 字段 | 说明 |
|---|---|
| 待迁移用户 | 钱包或 UID；系统自动识别主体类型 |
| 迁移到上级合伙人 | 目标上级钱包或 UID |
| 设置返佣比例 (%) | 迁移主体在新上级下的比例；UI 范围 1–80（与运营上限一致） |

输入后实时进入**预览区**（主体未识别时预览为空）。

**原型演示账号**（页内脚注，便于自测）：

| 类型 | 示例钱包 | 说明 |
|---|---|---|
| 普通用户 | `0xPlain...U1` | 仅直客 |
| 正常代理 | `0xMig...Ok` | 系统 L2，多条 5 层支路 |
| 链路倒挂代理 | `0xMig...Abn` | 系统 L6，B 支深层倒挂 |
| 目标上级 | `0xTo...L1` / `0xTo...L2` | 迁移接收方样例 |

### 2\.3\.2 主体类型与迁移范围

| 类型 | 识别结果 | 迁移范围 |
|---|---|---|
| 普通用户 | 非代理账号 | 仅该用户本人；预览展示其直客列表（分页） |
| 代理用户 | 代理账号 | **整伞迁移**：该代理及其全部下级合伙人、伞下全部直客一并挂到新上级 |

预览卡片展示：类型标签、钱包、UID、系统层级 Lx、链路规模、直客数量等。

### 2\.3\.3 预览：代理用户链路树

页面截图：[**后台 · 迁移预览（代理链路树）**](https://raw.githubusercontent.com/DashuaibiZZY/prototype/doc-assets/document/assets/%E5%90%88%E4%BC%99%E4%BA%BA/admin-migrate-preview.png)

- 以**迁移主体为根**展示代理链路树（相对标签：迁移主体 / 直接下级 / 二级下级…），并标注**系统 Lx**。

- 根节点比例 = 录入的「设置返佣比例」；根不可在树内改比例（改录入框）。

- 非根节点可在树内修改比例（用于修正倒挂或调整下级级差）。

- 迁移主体的**直接下级**支路分页（10 条/页）；更深层级 **+** 展开。

- 支持伞内**钱包 / UID 定位**（行为同返佣树：展开路径、翻页、高亮）。

- **倒挂检测**：若任一点 `子比例 > 父比例`，节点红色高亮，顶部横幅显示倒挂数量，提供「点击立即展开倒挂位置」。`子比例 = 父比例` 不视为倒挂。

### 2\.3\.4 提交前校验（必须全部通过）

| 校验项 | 规则 |
|---|---|
| 主体有效 | 待迁移用户可识别 |
| 目标上级有效 | 目标合伙人存在且可挂接 |
| 迁移比例 ≤ 目标上级比例 | 允许级差为 0；禁止迁移比例 &gt; 目标上级 |
| 迁移比例 ≤ 运营上限 | 超过须走其他特批流程（本页不允许直接提交） |
| 禁止挂在自己或自己的下级之下 | 防循环 |
| 代理用户整伞级差 | 应用所有预览中的比例覆盖后，全树满足「下级 ≤ 上级」 |
| 深层历史倒挂 | 与本次迁移比例无关的既有「下级 &gt; 上级」，须在树内先修正，倒挂计数为 0 才可提交 |

树内非根节点修改会收集为 **倒挂修正清单**（钱包、原比例 → 新比例），写入审批详情。

### 2\.3\.5 提交与审批

预览合法后，底部展示审核备注、图片附件与【提交风控审核】。

**迁移前异常伞提示**：代理用户且存在倒挂或暂停结算数据时，展示 §2.2.4「暂停结算待处理」模块（标题为「暂停结算待处理（迁移后将归属新上级统计与发放）」），含倒挂条数、暂停结算交易额/手续费汇总、明细表及每行【展开倒挂位置】操作。

确认弹窗汇总：待迁移用户、目标上级、迁移后比例、倒挂修正列表、备注、附件文件名。

页面截图：[**后台 · 迁移提交确认弹窗**](https://raw.githubusercontent.com/DashuaibiZZY/prototype/doc-assets/document/assets/%E5%90%88%E4%BC%99%E4%BA%BA/admin-modal-migrate-confirm.png)

提交后生成审批单「**返佣关系迁移**」，审批流：**市场运营提交 → 风控审核 → 通过即生效**（无需老板审批）。

**迁移后业绩与返佣归属**的业务规则见 §1.4「迁移后的返佣计算与业绩统计」。

---

## 2\.4 合伙人审核

**访问路径**：侧栏 → 「合伙人审核」（页内标题可为「合伙人配置审批」）

**页面作用**：处理三类合伙人配置变更审批；风控、老板切换角色 Tab 处理待办。

### 2\.4\.1 审批类型与流程

| 审批类型 | 触发场景 | 审批流 |
|---|---|---|
| 一级合伙人绑定（超上限） | 新增一级合伙人且比例 &gt; 运营上限 | 市场运营提交 → **风控** → **老板**（风控通过后同步 Lark） |
| 返佣比例调整（超出上限） | 返佣树提交且单项新比例 &gt; 运营上限 | 同上 |
| 返佣关系迁移 | 迁移返佣关系页提交 | 市场运营提交 → **风控** → 通过即生效 |

初始状态均为「待风控」。合伙人三类审批**无**市场运营交叉审核步骤。

### 2\.4\.2 列表筛选

| 筛选项 | 选项 |
|---|---|
| 视图 | 待我审批 / 全部审批 |
| 状态 | 全部、待交叉审核、待风控、待老板、已通过、已驳回 |
| 审批类型 | 一级合伙人绑定、返佣比例调整、返佣关系迁移 |
| UID / 钱包地址 | 模糊匹配主体用户 |
| 审批单号 | APR… |

**状态筛选项说明**：审批列表 UI 与全站审批组件共用，故下拉含「待交叉审核」。**合伙人三类审批实际流程不含市场运营交叉审核**（见 §2.4.1），合伙人单正常不会出现「待交叉审核」状态；筛选该选项通常无结果。

**角色 Tab**：风控、老板（根据当前 Tab 过滤「待我审批」）。

**列表列**：审批单号、类型、申请人、申请时间、钱包地址、UID、摘要、状态、【查看】/【审批】。

列表分页 10 条/页。

页面截图：[**后台 · 合伙人审核列表**](https://raw.githubusercontent.com/DashuaibiZZY/prototype/doc-assets/document/assets/%E5%90%88%E4%BC%99%E4%BA%BA/admin-approval-list.png)

### 2\.4\.3 审批详情

**通用区块**：审批单号、类型、申请人、申请时间、摘要、申请备注、审批时间线、审批进度图、 Lark 卡片（已同步时）、审批操作（通过 / 驳回；驳回须填意见）。

**一级合伙人绑定（超上限）详情**：

页面截图：[**后台 · 审批详情（一级绑定超上限）**](https://raw.githubusercontent.com/DashuaibiZZY/prototype/doc-assets/document/assets/%E5%90%88%E4%BC%99%E4%BA%BA/admin-approval-detail-bind.png)

- UID、钱包、申请返佣比例、运营配置上限、是否超上限。

- **图片附件**：缩略图网格，点击查看大图。

**返佣比例调整（超出上限）详情**：

页面截图：[**后台 · 审批详情（比例调整超上限）**](https://raw.githubusercontent.com/DashuaibiZZY/prototype/doc-assets/document/assets/%E5%90%88%E4%BC%99%E4%BA%BA/admin-approval-detail-ratio.png)

- UID、钱包、原/新返佣比例、运营上限、是否超上限、**修改原因备注**。

- **图片附件**（与绑定、迁移一致展示）。

**返佣关系迁移详情**：

页面截图：[**后台 · 审批详情（返佣关系迁移）**](https://raw.githubusercontent.com/DashuaibiZZY/prototype/doc-assets/document/assets/%E5%90%88%E4%BC%99%E4%BA%BA/admin-approval-detail-migrate.png)

- 待迁移用户、UID、类型（普通用户 / 代理用户）。

- 迁移到上级合伙人、迁移后返佣比例。

- **下级返佣修改**（倒挂修正列表）。

- **图片附件**。

---

## 2\.5 操作记录

**访问路径**：侧栏 → 「操作记录」（位于佣金对账与发放之下）

**数据来源**：合伙人三类审批的时间线事件聚合展示。

页面截图：[**后台 · 操作记录**](https://raw.githubusercontent.com/DashuaibiZZY/prototype/doc-assets/document/assets/%E5%90%88%E4%BC%99%E4%BA%BA/admin-logs.png)

### 筛选

| 筛选项 | 说明 |
|---|---|
| 操作人 | 后台账号（子串匹配） |
| 操作类型 | 全部、提交审批、返佣关系迁移、一级合伙人绑定、返佣比例调整、交叉审核通过、风控审核通过、老板审批通过、审批驳回 |
| 审批单号 | APR… |
| 审批类型 | 一级合伙人绑定、返佣比例调整、返佣关系迁移 |

**操作类型说明**：筛选项与全站审批组件共用。**合伙人三类审批无市场运营交叉审核**（§2.4.1），相关操作记录通常不会出现「交叉审核通过」。

### 列表列

时间、操作人、操作类型（标签样式）、审批单号、审批类型、摘要、备注。

分页 10 条/页。

---

## 2\.6 佣金对账与发放页（风控用）

**访问路径**：侧栏 → 「佣金对账与发放」

**页面作用**：每日结算批次的审查、实发/补发人工调整、最终放款或拒绝。

### 2\.6\.1 结算批次列表

**筛选**：结算日期（日期选择器）、批次状态（全部 / 等待对账 / 已拒绝）；查询 / 重置。

| 列名 | 数据定义 |
|---|---|
| 结算日期 | 结算周期对应日期 |
| 返佣总成交额 | 当日参与返佣计算的总成交额 |
| 补发修正后发放金额 | 该**发放日**执行的修正返佣补发合计 |
| 今日待发放总佣金 | 当日批次中无需「待修正」的实发佣金合计 |
| 合计待发放 | 补发修正 + 今日待发放 |
| 状态 | `等待对账` / `已拒绝` |
| 操作 | 【查看详情】；【查看原因】（已拒绝批次） |

分页 10 条/页。进入本页默认展示批次列表视图。

页面截图：[**后台 · 结算批次列表**](https://raw.githubusercontent.com/DashuaibiZZY/prototype/doc-assets/document/assets/%E5%90%88%E4%BC%99%E4%BA%BA/admin-settlement-list.png)

### 2\.6\.2 审核详情页

**顶部**：

- 【← 返回】批次列表。

- 【下载对账单】：导出该批次 **5 个**对账 CSV（表 1、表 2、表 2a、表 3、表 4，见 §2.6.5）。

    - **频控**：同一操作人对同一批次（或全局对账导出）**每 10 分钟最多下载 1 次**；过于频繁时提示剩余等待时间，避免对数据库造成压力。

- 【拒绝放款】、【审核通过并放款】。

**横幅**：

- 已拒绝：红色横幅「该批次已被拒绝放款」。

- **异常代理横幅**（`abnormal-banner`，仅当批次含待修正代理时显示）：

    - 文案：「检测到该批次包含 N 个异常代理记录。原因：下级配置比例超过该级代理。受影响代理的佣金已自动置为暂停结算状态。」

    - 按钮：【导出异常明细】— 下载 `Abnormal_Agents_{结算日期}.csv`（字段见 §2.6.5）。

- 无异常代理时该横幅隐藏。

**Tab：批次明细**

**筛选**：合伙人（钱包/UID）、层级 L1–L5、待修正返佣（是/否）、已修改实发（是/否）；重置；【批量修改实发】。

| 列名 | 说明 |
|---|---|
| 合伙人 | 钱包、UID |
| 层级 | Lx |
| 返佣比例 | |
| 上级 | 上级钱包；一级显示「一级」 |
| 待修正 | 是否待修正返佣后计算 |
| 成交额 | |
| 原始佣金 | 待修正时显示「待修正返佣后计算」及原应结日 |
| 实发 | 人工调整后的实发；低于原始时标「已调减」 |
| 操作 | 【修改】；待修正行不可改实发 |

**Tab：修正返佣补发**

**适用范围（重要）**：

| 原应结日所属批次状态 | 是否进入本 Tab |
|---|---|
| **已审核通过并放款** | 是。该分支当时为 `待修正返佣后计算` 而未实发的部分，修正比例后在此按**原应结日**挂账补发 |
| **等待对账** | 否。待修正行仍在「批次明细」中展示为待修正；须先修正比例，再在该批次审核通过时按修正后比例计入**当日实发**（或批次内重算后放款） |
| **已拒绝** | 否。不产生补发记录；批次打回重算，修正后随**新批次**重新对账与发放 |

即：**修正返佣补发仅处理「原批次已放款、但因异常未实发」的欠发**；等待对账与已拒绝批次在**后续完成对账/重算并放款**时处理，不走补发 Tab。

**筛选**：合伙人、**原应结日**（日期）；重置；【批量修改补发】。

| 列名 | 说明 |
|---|---|
| 合伙人 | 钱包、UID |
| 原应结日 | 异常暂停前原本应结算的日期 |
| 原始返佣 | 按修正后比例重算前的参考值 |
| （实际）补发佣金 | 可人工调减；低于原始时标「已调减」 |
| 操作 | 【修改】 |

两个 Tab 内表格均分页 10 条/页。

页面截图：[**后台 · 批次审核详情**](https://raw.githubusercontent.com/DashuaibiZZY/prototype/doc-assets/document/assets/%E5%90%88%E4%BC%99%E4%BA%BA/admin-settlement-detail.png)

页面截图：[**后台 · 修正返佣补发 Tab**](https://raw.githubusercontent.com/DashuaibiZZY/prototype/doc-assets/document/assets/%E5%90%88%E4%BC%99%E4%BA%BA/admin-settlement-supplement-tab.png)

### 2\.6\.3 人工调整规则

| 场景 | 规则 |
|---|---|
| 修改实发 | 实发不得高于**原始佣金**；`待修正返佣后计算` 行禁止修改 |
| 修改补发 | 补发不得高于**原始返佣** |
| 单条修改 | 弹窗输入金额保存 |
| 批量修改 | 独立批量页：支持手工逐行或上传 CSV/TXT/XLSX |

页面截图：[**后台 · 修改实发佣金弹窗**](https://raw.githubusercontent.com/DashuaibiZZY/prototype/doc-assets/document/assets/%E5%90%88%E4%BC%99%E4%BA%BA/admin-modal-edit-actual.png)

页面截图：[**后台 · 修改补发佣金弹窗**](https://raw.githubusercontent.com/DashuaibiZZY/prototype/doc-assets/document/assets/%E5%90%88%E4%BC%99%E4%BA%BA/admin-modal-edit-supplement.png)

**批量格式**：

- 实发：`钱包或UID,实发金额`

- 补发：`钱包或UID,原应结日,补发金额`

批量提交后展示逐行成功/失败报告。

### 2\.6\.4 放款与拒绝

- **审核通过并放款**：确认后按对账后金额执行发放，批次状态更新（原型为演示提示）。

- **拒绝放款**：必填拒绝原因；批次标记已拒绝，返回列表。

### 2\.6\.5 对账单数据逻辑定义（.csv）

#### 待修正数据在对账中的处理

- 批次明细中标记为 `待修正返佣后计算` 的合伙人行：**不参与**当日「今日待发放总佣金」的实发合计；表 4（System\_Proposed\_Payout）中对应行原始佣金为待修正状态，**最终实发为 0**（或整行不进入可发放集合）。

- 表 3（分账明细流水）中，异常分支在暂停期间**不产生**可发放的获得者汇总（或产生但标记为暂停，不进入表 4 可发放汇总）。

- 风控在总量对账时须区分：**全站返佣总预算**含理论应计 vs **可发放预发放**排除待修正分支，避免误将暂停分支计入放款总额。

#### 导出文件清单

点击审核详情【下载对账单】，依次下载以下 5 个 CSV（UTF-8 BOM，逗号分隔）：

| 文件名 | 说明 |
|---|---|
| `Platform_Summary_{日期}.csv` | 表 1 平台总交易对账 |
| `Affiliate_Map_{日期}.csv` | 表 2 返佣关系映射 |
| `L1_Rebate_Ratio_Snapshot_{日期}.csv` | 表 2a 一级返佣比例对账 |
| `Rebate_Split_Detail_{日期}.csv` | 表 3 分账明细流水 |
| `System_Proposed_Payout_{日期}.csv` | 表 4 系统预发放汇总 |

点击【导出异常明细】，下载 `Abnormal_Agents_{日期}.csv`（仅含本批次 `待修正返佣后计算` 的代理行）。

#### 表 1：平台总交易对账表 (Platform\_Summary\.csv)

*宏观对账：验证「大池子」是否算错。*

| CSV 列名 | 说明 |
|---|---|
| 结算日期 | YYYY-MM-DD |
| 计算周期开始 | 如 2024-05-23 00:00:00 |
| 计算周期结束 | 如 2024-05-23 23:59:59 |
| 平台总成交额(USDT) | 当日参与返佣计算的总成交额 |
| 全站可参与返佣手续费_NetFee(USDT) | 剔除做市商后的净手续费 |
| 全站返佣总预算_TotalPayout(USDT) | 理论应计返佣总和（含待修正分支的应计，见表 4） |
| 返佣手续费占比(%) | TotalPayout / NetFee |
| 平台留存收入(USDT) | NetFee − TotalPayout |
| 本批次可发放实发合计(USDT) | 排除待修正行后的实发合计（对应后台「今日待发放总佣金」） |

**怎么用**：校验 `平台留存 + 全站返佣总预算 ≈ NetFee`；可发放实发 ≤ 全站返佣总预算。

#### 表 2：返佣关系映射表 (Affiliate\_Map\.csv)

*关系字典：核对层级与比例是否合规。*

| CSV 列名 | 说明 |
|---|---|
| 结算日期 | |
| 用户地址 | 合伙人钱包 |
| 直接上级地址 | 一级显示 — |
| 顶级一级代理地址 | Root |
| 用户当前等级 | 1=一级… |
| 用户返佣比例(%) | |
| 上级返佣比例(%) | 无上级为 — |
| 级差空间(%) | 上级比例 − 用户比例；负值表示倒挂 |
| 配置状态 | `正常` / `待修正返佣后计算` |
| 完整路径 | Root / … / Current |

**怎么用**：筛选 `配置状态=待修正` 定位异常分支；核对级差空间非负（允许 0）。

#### 表 2a：一级合伙人返佣比例对账表 (L1\_Rebate\_Ratio\_Snapshot\.csv)

*一级视角快照：子树比例错误时，仍可按一级「佣金包」做宏观上限校验。*

| CSV 列名 | 说明 |
|---|---|
| 结算日期 | 与批次一致 |
| 一级合伙人地址 | 该业务线 L1 Root 钱包 |
| 一级合伙人UID | |
| 一级返佣比例(%) | L1 当前配置比例（该线总佣金包上限参考） |
| 所属一级下伞内净手续费_NetFee(USDT) | 该结算日内归属该 L1 业务线的可参与返佣净手续费（按本批次明细行汇总） |
| 一级理论最大原始应发佣金(USDT) | `伞内 NetFee × 一级返佣比例 ÷ 100`（**不拆下级级差**，表示该线返佣成本上限） |
| 伞内待修正分支数 | 该 L1 下本批次中 `待修正` 的代理行数 |
| 备注 | 如有待修正分支，提示「存在待修正分支，须结合表2穿透」 |

**怎么用（建议流程）**：

1. 对每一行 L1，比较 **一级理论最大原始应发** 与表 4 中该 Root 下各代理「待结算返佣总额」之和：后者不应长期、大幅超过前者（否则子树拆分或比例配置可能有误）。

2. 若 **伞内待修正分支数 &gt; 0**：表 3 / 表 4 中对应分支可能暂停或实发为 0，须用表 2 下钻倒挂点，修正后再对账或补发。

3. 与表 1 联动：各 L1 的 NetFee 之和应接近表 1 全站 NetFee（按业务线拆分核对）。

4. **典型场景**：某四级子树比例倒挂，表 4 单行不可信；用本表该 L1 的「理论最大原始应发」作为**整线返佣上限参考**，再结合表 3 穿透异常子树。

#### 表 3：分账明细流水表 (Rebate\_Split\_Detail\.csv)

| CSV 列名 | 说明 |
|---|---|
| 结算日期 | |
| 源_产生交易用户地址 | 交易来源人 |
| 源_该用户期间成交额(USDT) | |
| 源_该用户产生手续费_NetFee(USDT) | |
| 源_该用户的上级地址 | |
| 源_该用户的顶级一级代理 | |
| 分_佣金获得者地址 | |
| 分_分账角色类型 | SELF / DIRECT_GAP / INDIRECT_GAP |
| 分_分账级差比例(%) | |
| 分_最终分账金额(USDT) | 待修正分支为 0.00 |
| 分账状态 | `正常` / `暂停结算` |

**怎么用**：枢纽表按「分_佣金获得者地址」汇总；`暂停结算` 行不计入可发放汇总。

#### 表 4：系统预发放汇总表 (System\_Proposed\_Payout\.csv)

| CSV 列名 | 说明 |
|---|---|
| 结算日期 | |
| 代理地址 | |
| 所属顶级代理(Root) | |
| 代理等级 | |
| 待结算返佣总额(USDT) | 待修正行填应计预估值 |
| 人工调减金额(USDT) | original − actual |
| 最终实发金额(USDT) | 待修正为 0 |
| 待修正返佣后计算 | 是 / 否 |

**怎么用**：与表 3 汇总比对差异为 0；待修正行「最终实发」必须为 0。

#### 异常代理明细 (Abnormal\_Agents\.csv)

| CSV 列名 | 说明 |
|---|---|
| 结算日期 | |
| 合伙人钱包 | |
| UID | |
| 层级 | Lx |
| 返佣比例(%) | |
| 上级钱包 | |
| 待修正原因 | 如下级配置比例超过该级代理 |
| 原应结日 | |
| 暂停结算成交额(USDT) | 暂停期间成交额 |
| 暂停结算手续费(USDT) | 暂停期间手续费 |

**说明**：异常暂停期间**不导出应发返佣金额**（尚未计算）；修正比例并恢复结算后，在批次实发或修正返佣补发中体现。

**怎么用**：风控从批次详情一键导出，与表 2 / 批次明细待修正行交叉核对。

#### **风控实操案例（审计流程）**

**第一步：总量对账（宏观核减）** — 打开表 1，校验净手续费与「平台留存 + 返佣总预算」守恒；结合表 2a 按 L1 校验各业务线是否超「一级理论最大原始应发」。

**第二步：系统聚合校验** — 对表 3 做枢纽分析，按获得者地址汇总分账金额（排除或单独标记待修正暂停流水）。

**第三步：差异比对** — 表 4 与第二步结果 VLOOKUP，差异列须全为 0；待修正行实发应为 0 直至修正后补发或批次内重算。

**第四步：穿透抽样** — 抽检代理，核对表 3 分账比例与表 2 级差关系一致；异常分支用表 2a + 表 2 定位 L1 与倒挂点。

---

# 用户合伙人中心（Web）

**产品原型**：[合伙人中心](https://dashuaibizzy.github.io/prototype/perp_dex/代理中心.html)

面向已开通合伙人资格的登录用户，用于管理邀请链接、查看团队业绩、维护下级合伙人、查询佣金结算。对外文案统一使用「合伙人」，不使用「代理」。

**与「邀请返佣」页的关系**：普通邀请返佣页面向非合伙人用户；**合伙人用户登录后应进入本中心**。若强行打开邀请返佣页，展示占位并引导跳转至合伙人中心（见《[邀请返佣（Web_APP）](邀请返佣（Web_APP）.md)》）。

---

## 3\.0 整体布局与导航

### 侧栏

| 入口 | 说明 |
|---|---|
| **邀请链接管理** | **默认首页**（登录后首次进入本中心时展示） |
| **合伙人管理** | 团队业绩、下级名单、穿透查看 |
| **佣金结算记录** | 佣金汇总与每日结算流水（页内标题为「佣金结算与发放」；侧栏原型文案为繁体「佣金結算記錄」，产品统一为简体） |

侧栏底部固定展示 **我的最高比例**：当前账号被平台/上级授予的返佣比例上限（可配置项；原型演示为 70%）。下级配置、调整比例均不得超过该上限。

### 通用交互

| 约定 | 说明 |
|---|---|
| 统计周期 | 各业务页支持 `1D` / `1W` / `1M` / `3M`（**不含 ALL**；`ALL` 仅运营后台列表/详情/穿透，见 §2 通用约定）；选中项高亮。切换后刷新该页周期敏感指标 |
| 分页 | 邀请链接列表、直属下级表、直客表、团队穿透内各表、每日结算流水均为 **每页 10 条**，底部分页器翻页 |
| 排序 | 邀请链接列表、合伙人下级表支持点击列头排序（交易额、手续费、返佣、净入金、级差收入、用户规模等，以页面展示列为准） |
| 复制 | 钱包地址、备注、邀请链接等提供一键复制；复制成功有提示 |
| 地址展示 | 列表中钱包地址缩写展示；复制时写入完整地址 |
| 页面切换 | 侧栏切换各业务页（邀请链接 / 合伙人管理 / 佣金结算记录）；**无** URL Hash 路由（与运营后台不同，见 §2 页面路由） |

---

## 3\.1 邀请链接管理（默认页）

**页面作用**：创建多条邀请链接，按渠道追踪推广效果；管理备注与邀请码。

页面截图：[**Web · 邀请链接管理**](https://raw.githubusercontent.com/DashuaibiZZY/prototype/doc-assets/document/assets/%E5%90%88%E4%BC%99%E4%BA%BA/web-links-page.png)

### 3\.1\.1 页头与创建入口

- 标题「邀请链接管理」；说明支持多链接分渠道统计。

- 【+ 創建新連結】展示已用数量上限（如「已用 12/50」），点击打开创建弹窗。

### 3\.1\.2 统计看板（页内工具条）

| 字段 | 说明 |
|---|---|
| 当前最高返佣比例 | 与侧栏「我的最高比例」一致 |
| 我的推荐人 | 绑定我成为合伙人时的上级钱包（缩写） |
| 默认邀请码 | 系统默认链接的 6 位邀请码，支持复制 |
| 【分享海报】 | 打开展示海报弹窗，含默认邀请码与二维码位，可保存图片 |

页面截图：[**Web · 分享海报弹窗**](https://raw.githubusercontent.com/DashuaibiZZY/prototype/doc-assets/document/assets/%E5%90%88%E4%BC%99%E4%BA%BA/web-links-share-modal.png)

### 3\.1\.3 链接列表

**工具条**：

- 统计周期：`1D` / `1W` / `1M` / `3M`（影响列表内交易额、手续费、返佣收入等周期指标）。

- 搜索：备注名称或邀请码模糊匹配。

**表格列**（不提供「邀请链接」整列；通过操作复制完整链接）：

| 列名 | 说明 |
|---|---|
| 备注名称 | |
| 邀请码 | 6 位字母数字，唯一 |
| 直邀人数 | 通过该码直接注册的直客数 |
| 下级合伙人数 | 通过该码发展出的下级合伙人数 |
| 总交易额 | 当前周期内，该码归因伞下成交额 |
| 合计手续费 | 当前周期内对应手续费 |
| 合计返佣收入 | 当前周期内该码归因返佣 |
| 总净入金 | 该码归因伞下净入金 |
| 操作 | 【复制链接】（完整 URL，含 `ref=邀请码`）；【修改备注】（编辑模式不可改邀请码） |

默认链接不可删除；非默认链接可修改备注。

### 3\.1\.4 创建 / 修改邀请链接弹窗

页面截图：[**Web · 创建邀请链接弹窗**](https://raw.githubusercontent.com/DashuaibiZZY/prototype/doc-assets/document/assets/%E5%90%88%E4%BC%99%E4%BA%BA/web-modal-referral-link.png)

| 字段 | 规则 |
|---|---|
| 备注名称 | 选填，最长 20 字 |
| 自定义邀请码 | **创建时必填**；**必须 6 位**字母或数字；不可与已有码重复；创建后不可修改 |
| 提交 | 创建成功后列表刷新；修改模式仅保存备注 |

---

## 3\.2 合伙人管理

**访问路径**：侧栏 → 「合伙人管理」

**页面作用**：查看本人伞下业绩构成，管理直属下级合伙人，处理比例异常提示。

页面截图：[**Web · 合伙人管理**](https://raw.githubusercontent.com/DashuaibiZZY/prototype/doc-assets/document/assets/%E5%90%88%E4%BC%99%E4%BA%BA/web-overview.png)

### 3\.2\.1 全局时间周期

- 位置：页头右侧。

- 选项：`1D` / `1W` / `1M` / `3M`。

- 影响：本页所有指标卡片、下级表、直客表中的周期数据（交易额、返佣预估、级差收入等）。

### 3\.2\.2 我的上级 / 我的返佣比例（顶部信息条）

位于指标卡片之上，左右分栏：

| 区域 | 说明 |
|---|---|
| **我的上级** | 若我为一级合伙人（无上级），展示 **一级代理**；否则展示上级钱包（可复制完整地址） |
| **我的返佣比例** | 当前账号生效的返佣比例数值（如 70%），与侧栏「最高比例」可能相同或为其下级配置上限内的当前值 |

### 3\.2\.3 核心指标卡片（4 张）

| 卡片 | 数据定义 |
|---|---|
| 团队总交易额 | 本人 + 全部下级合伙人 + 全部直客，在选定周期内的总成交额（含下级穿透） |
| 总返佣收入（本期预估） | 本期预计返佣总和；副栏拆分：**自返佣**、**直客**、**级差**。若后台已对某日佣金做**实发调减**，本卡片及周期汇总按**修正后实发**口径展示（见 §1.5） |
| 团队净入金 | 整条线净充值（充值 − 提现），含下级穿透 |
| 用户规模 | **主行**：交易用户数（周期内交易额 &gt; 0）；**次行灰色**：总用户数 |

**原型说明**：「团队总交易额」卡片下方 **较上周期 +X%** 为演示占位文案，正式环境需接真实环比或隐藏。

### 3\.2\.4 团队返佣异常横幅

当本人伞下存在**团队返佣树异常**（深层链路比例倒挂，见 §1.4）时，在指标卡片与表格之间展示琥珀色横幅：

- 文案包含：异常返佣线条数、暂停结算交易额合计。

- 引导用户在下级表中查看异常明细，并与下级沟通调整比例。

**说明**：横幅描述的是**树形异常**场景；**直属下级比例高于我**的单层倒挂不在此横幅汇总，而在对应下级行的结算状态中体现。

**展示规则**：仅当伞下存在团队返佣树异常时展示；无异常时隐藏（原型静态页可能常显演示文案，以接口数据为准）。

### 3\.2\.5 数据明细区（Tab）

表格区顶部：

- Tab：**直属下级合伙人明细**（默认）、**自邀直客明细**。

- **结算状态筛选**（仅直属下级 Tab）：全部结算状态 / 正常 / 返佣比例倒挂（直属下级）/ 团队返佣树异常。

- **搜索**：钱包地址或备注（直属下级 Tab）；直客 Tab 按钱包搜索。

- 【+ 新增下级合伙人】：打开绑定弹窗。

#### Tab：直属下级合伙人明细

| 列名 | 说明 |
|---|---|
| 加入时间 | 绑定时间 |
| 下级合伙人（备注） | 钱包 + 复制；有备注时展示备注 + 复制 |
| 设置比例 | 我为该下级配置的比例；**直属倒挂**时红色高亮 |
| 我的级差 | `我的比例 − 下级比例`；相等为 0 正常；下级 &gt; 我时为倒挂（红色负值） |
| 结算状态 | 见下表「用户侧结算状态」 |
| 贡献级差收入 | 周期内该下级为我带来的级差收入；倒挂或树异常且该分支停结时展示 **-- 暂停结算**；正常分支展示金额 |
| 总交易额 | 该下级向下伞成交额（当前周期） |
| 总净入金 | 该下级向下伞净入金 |
| 用户规模 | 主行交易用户 / 次行总用户 |
| 操作 | 【查看团队】进入团队穿透（见 §3.3）；【调整】打开比例调整弹窗（见 §3.2.7） |

**用户侧结算状态（直属下级行）**：

| 状态 | 含义 | 行内展示要点 |
|---|---|---|
| 正常 | 无倒挂、无树异常 | 结算状态列为「—」或正常 |
| 返佣比例倒挂（直属下级） | 该直属下级比例 **&gt; 我** | 红色文案「返佣比例已高于您，请立即调整」；设置比例列红色；贡献级差暂停 |
| 团队返佣树异常 | 该下级伞内存在深层 `下级 &gt; 上级` | 可点击链接，文案含异常条数及「交易额 $XX 停结」；打开 §3.2.8 弹窗 |

#### Tab：自邀直客明细

| 列名 | 说明 |
|---|---|
| 注册时间 | |
| 直客钱包地址 | 缩写 + 复制 |
| 累计交易额 | 当前周期 |
| 累计手续费 | 当前周期 |
| 返佣金额 | 我从该直客获得的返佣（表头文案为「返佣金额」） |
| 净入金 | |
| 操作 | 【设置为下级合伙人】（仅合伙人管理页；团队穿透页无此列） |

两 Tab 均分页（10 条/页）。

### 3\.2\.6 新增下级合伙人弹窗

页面截图：[**Web · 新增下级合伙人弹窗**](https://raw.githubusercontent.com/DashuaibiZZY/prototype/doc-assets/document/assets/%E5%90%88%E4%BC%99%E4%BA%BA/web-modal-add-agent.png)

| 字段 | 规则 |
|---|---|
| 下级钱包地址 / UID | 必填 |
| 配置返佣比例 | **滑动条**调节，范围 0%～侧栏「我的最高比例」；滑动条旁实时展示百分比；原型演示步进为 5%、上限 70% |
| 备注名称 | 选填 |
| 确认绑定 | 校验通过后生效；存在直属倒挂或已达第 10 层时禁止提交（见 §3.5） |

**业务限制**（与 §1.4 一致）：

- 若我存在**直属下级倒挂**（有下级比例 &gt; 我），禁止新增下级，须先调整比例。

- 若我已达平台 **第 10 层**合伙人深度，禁止再绑定新的下级合伙人（直客不受限）。

- 新下级比例不得超过我的比例。

### 3\.2\.7 调整返佣比例弹窗

从直属下级行【调整】进入。

页面截图：[**Web · 调整返佣比例弹窗**](https://raw.githubusercontent.com/DashuaibiZZY/prototype/doc-assets/document/assets/%E5%90%88%E4%BC%99%E4%BA%BA/web-modal-adjust-ratio.png)

| 元素 | 说明 |
|---|---|
| 下级信息 | 钱包（可复制）；有备注时展示备注行（可复制） |
| 返佣比例 | **数字输入框**与**滑动条**联动，范围 0～我的最高比例，步进 1%；右侧实时展示百分比 |
| 风险提示 | 琥珀色提示区：若新比例 **&lt; 该下级名下最高下级比例**，提示将触发倒挂并暂停相关交易额结算，须确认已与下级沟通 |
| 确认调整 | 保存后刷新列表；若调整后满足 `下级 ≤ 我` 且伞内无其他倒挂，相关异常状态可恢复 |

### 3\.2\.8 团队返佣树异常明细弹窗

从下级行「团队返佣树异常」链接触发。

页面截图：[**Web · 团队返佣树异常明细弹窗**](https://raw.githubusercontent.com/DashuaibiZZY/prototype/doc-assets/document/assets/%E5%90%88%E4%BC%99%E4%BA%BA/web-modal-team-tree.png)

| 元素 | 说明 |
|---|---|
| 标题 | 团队返佣树异常明细 |
| 副标题 | 异常下级钱包、异常线条数 |
| 【展开全部】 | 一键展开所有异常线详情 |
| 异常线列表 | 每条可展开/收起；展示链路上各节点钱包、备注、比例 |
| **停止结算交易额** | **每条异常线独立展示**该线暂停结算的成交额合计 |

穿透页内点击异常链接触发时，钱包与备注按**脱敏规则**展示（见 §3.3）。

---

## 3\.3 团队穿透（查看团队）

从直属下级【查看团队】进入；支持**无限向下穿透**（下级合伙人的下级仍可【查看团队】）。

页面截图：[**Web · 团队穿透页**](https://raw.githubusercontent.com/DashuaibiZZY/prototype/doc-assets/document/assets/%E5%90%88%E4%BC%99%E4%BA%BA/web-drill-overview.png)

### 3\.3\.1 页头

- 【← 返回】：若仅穿透一层则回到「合伙人管理」；若多层穿透则回到上一层团队页。

- 标题：`{合伙人名称} 的团队`；副标题含脱敏钱包、加入时间、当前穿透层级（多层时）。

- 统计周期：同 §3.2.1。

### 3\.3\.2 我的上级 / 我的返佣比例

与 §3.2.2 **同款布局**，但展示对象为**当前被查看的合伙人**（非登录用户本人）：

| 区域 | 说明 |
|---|---|
| 我的上级 | 该合伙人的直属上级钱包（**脱敏**，不提供复制） |
| 我的返佣比例 | 该合伙人当前生效比例 |

向下穿透时，上级与比例随当前焦点合伙人变化。

### 3\.3\.3 指标卡片与横幅

与「合伙人管理」同构的四张指标卡片（团队总交易额、总返佣收入及拆分、团队净入金、用户规模），数据范围为**该合伙人向下伞**。

若该合伙人伞下存在树异常，展示与 §3.2.4 类似的琥珀色横幅（文案为该伞口径）；无异常时隐藏。

### 3\.3\.4 明细表格

- Tab 与列与 §3.2.5 一致，但：

  - 钱包、备注**脱敏展示**，无复制按钮（穿透页只读）。

  - 无【+ 新增下级合伙人】、无【调整】操作。

  - 仍有【查看团队】（若该下级仍有团队且允许穿透）。

- **无**【设置为下级合伙人】列（该操作仅在 §3.2 合伙人管理页直客表出现）。

- 结算状态筛选、搜索、分页规则相同。

---

## 3\.4 佣金结算与发放

**访问路径**：侧栏 → 「佣金结算记录」

**页面标题**：佣金结算与发放

**页面作用**：展示佣金汇总与按日结算流水；让用户知晓每日交易额、返佣金额与结算状态。

页面截图：[**Web · 佣金结算与发放**](https://raw.githubusercontent.com/DashuaibiZZY/prototype/doc-assets/document/assets/%E5%90%88%E4%BC%99%E4%BA%BA/web-settlement.png)

### 3\.4\.1 汇总卡片（3 张）

| 卡片 | 说明 |
|---|---|
| 待结算佣金（今日预估） | 当前自然日预估、尚未进入最终放款的佣金 |
| 已结算总额（累计） | 历史已成功发放累计 |
| 昨日发放佣金 | 最近一个已完成发放日的实发金额及成功状态 |

### 3\.4\.2 每日结算流水

**筛选**：

- 结算日期（日期选择器，按日过滤）。

- 结算状态：全部 / 待结算 / 已结算 / 返佣异常停止结算。

**表格列**：

| 列名 | 说明 |
|---|---|
| 结算日期 | 对应自然日（日结周期见 §1.3） |
| 交易额 | 当日参与返佣计算的有效成交额 |
| 返佣金额 | 当日应展示给用户的返佣金额（**已调减实发时展示修正后实发**）；若存在违规扣减，主行下方红色小字 **违规 −$XX** |
| 结算状态 | 见下表 |

**用户侧结算状态（每日流水）**：

| 展示状态 | 业务含义 |
|---|---|
| **待结算** | 该日佣金尚未完成放款，含：批次等待对账、或后台批次**已拒绝**打回重算（用户侧统一显示待结算，不展示「已拒绝」） |
| **已结算** | 该日佣金已审核通过并完成发放 |
| **返佣异常停止结算** | 该日存在因比例异常暂停结算的交易额：**交易额 &gt; 0，返佣金额为 0**（返佣树异常或相关分支停结） |

分页：10 条/页。

### 3\.4\.3 与后台对账的对应关系（产品口径）

| 后台事实 | 用户流水展示 |
|---|---|
| 批次等待对账 | 待结算 |
| 批次已拒绝 | 待结算（不暴露拒绝） |
| 批次已放款 | 已结算 |
| 当日分支停结 / 待修正 | 返佣异常停止结算；交易额有值、返佣 0 |
| 风控调减实发 | 返佣金额按调减后实发；合伙人管理总返佣收入同步按实发累计 |
| 违规扣减 | 返佣金额下展示「违规 −$XX」 |

补发、修正比例后的历史欠发：在用户侧体现为对应日的返佣金额更新或后续日期的已结算金额，不单独使用后台「补结算」等内部状态文案（若需对用户解释，可用「已结算」+ 金额变化或帮助说明）。

---

## 3\.5 用户侧异常与恢复（摘要）

与 §1.4 一致，合伙人中心仅做**提示与引导**，修正操作在「调整比例」或联系下级完成：

1. **直属倒挂**：将直属下级比例调至 **≤ 我的比例** 后，该直属分支恢复结算展示。

2. **团队返佣树异常**：在异常弹窗查看各线 **停止结算交易额**，协调下级调整深层比例；全部修正后停结解除，历史停结交易额按规则重算（未放款批次内重算或走补发，用户侧最终看到修正后返佣金额）。

3. **新增下级**：自身存在倒挂或已达第 10 层时系统禁止绑定新下级合伙人。
