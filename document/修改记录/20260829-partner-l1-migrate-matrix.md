# 2026-08-29 · 一级升级身份预览 & 迁移组合表

## 8. 允许所有人升级为一级合伙人

- `admin-partner-portal.js` / 绑定弹窗：输入 UID/钱包后自动识别身份（普通用户 / 合伙人直客 / N 级代理 / 已是一级）。
- 按身份展示不同预览：直客列表 vs 整伞返佣树摘要；仅需配置**一级返佣比例**。
- 审批 payload / 详情增加 `subjectKind`、`upgradeScope` 等字段。

## 9. 两类返佣关系变更 · 文档 + 原型

- `邀请合伙人无限层返佣系统.md` §1.4：补充「两类动作」、**原上级数据边界**、迁移主体×目标组合表、升级主体差异表。
- 迁移原型：目标支持普通用户 `0xPlain...Host`；挂到普通用户下级时固定直客、隐藏身份选择与比例。
- 迁移主体扩展：合伙人直客 `0xde...55aa`、N 级 `0xNorm...L3` 等；校验禁止代理挂到普通用户下级。

## Cache

- `partner-demo-26`
